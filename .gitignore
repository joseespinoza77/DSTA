# Ignorar todo en la raíz por defecto
/*

# ─── Gemelo Digital (GitHub Pages) ────────────────────────────────────────────
# Solo se publican los archivos del visor 3D estático.
# Three.js se carga desde CDN; no se necesita ningún servidor Python.

!/web/

# Archivos de conveniencia en la raíz
!/.gitignore
!/README.md
!/index.html

# Excluir todo lo demás: scripts Python, planillas, planos, manuales, GIS, etc.
# (quedan en el repositorio local pero no se publican)
