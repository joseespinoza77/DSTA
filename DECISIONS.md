# Registro de Decisiones (DECISIONS.md)

## DSTA - EDAR Silvouta

| Fecha | Decisión | Motivo | Consecuencias |
|-------|----------|--------|---------------|
| 2026-05-20 | Migración a formato texto de manuales | Permitir que la IA pueda leer los documentos PDF (que estaban fallando por OCR o NotebookLM expirado) | Se han creado archivos .txt en `DSTA/manual/txt`. |
| 2026-05-20 | Cambio de geometría abstracta a ortogonal | Las tuberías "voladoras" no representaban la realidad física de la planta. | El modelo 3D ahora usa colectores ortogonales de 90 grados y ramales, simulando construcción civil real. |
| 2026-05-20 | Modificación del Layout del Excel a 4 Líneas y Decantación Rectangular | Para cuadrar matemáticamente con las exigencias del pliego técnico y la imagen de planta real (`layout_12_3.jpeg`). | Se actualizaron las fórmulas de `generate_edar_excel.py` para prismas rectangulares. |
| 2026-05-21 | Ordenamiento de la Línea de Fangos (Flujo Hidráulico Sur) | Las tuberías de lodos iban "hacia atrás" en el eje X. | Se reestructuraron las coordenadas Z y X de la zona sur (Espesadores, DAF, Digestores, Deshidratación) para que fluyan de izquierda a derecha en paralelo a la línea de agua. |
| 2026-05-21 | Inclusión de Gradiente de Colores de Depuración | Mejorar la interpretación visual de las fases de depuración y digestión de fangos en el visor 3D. | El JSON de geometría incluye ahora colores hexadecimales dinámicos según el avance de la depuración. |
| 2026-05-21 | Inclusión de Tornillos de Arquímedes | Simular la obra de llegada desde colectores profundos con el máximo rigor. | Se programó una nueva función `add_diagonal_pipe` apoyada por un algoritmo de rotación Quaternion en Three.js. |
| 2026-05-21 | Juntas visibles y colectores explícitos en tuberías 3D | La vista 3D mostraba cortes aparentes en tuberías aunque algunas conexiones existían como coordenadas implícitas. | `scripts/generate_digital_twin.py` añade nodos esféricos en extremos/codos, colector de salida de biológicos y colector sur WAS antes de regenerar `web/data/geometry.json`. |
| 2026-05-21 | Contenedor de gruesos junto al pozo | La obra de llegada necesitaba un punto visible para que la cuchara bivalva descargue los residuos gruesos. | Se incorpora la línea visual `rechazos` con un contenedor abierto y sólidos retenidos al lado del pozo de gruesos. |
| 2026-05-21 | Uso de `real.pdf` como contrato de auditoría de líneas | El usuario aportó el PDF real para asegurar que las líneas de agua, fangos, biogás y rechazos no se validen solo por inspección visual. | Se extrajo `manual/txt/proyecto_real_real.txt`, se renderizaron las páginas 133 y 134, y se creó `LINEAS_PROCESO_REAL.md`. |
| 2026-05-21 | Auditor reproducible de líneas del Gemelo Digital | La corrección visual de tuberías no garantiza conformidad de proceso. | Se añadió `scripts/audit_digital_twin_lines.py`; el JSON queda geométricamente sano, pero el proceso actual no es conforme al PDF real por ausencia de MBR, hidrólisis térmica, escurridos, cogeneración y otros equipos. |
| 2026-05-21 | Eliminación de tramos superpuestos exactos | Algunos ramales repetían el mismo tramo de colector y podían generar sobredibujado visual. | `generate_digital_twin.py` usa colectores comunes únicos antes de ramificar hacia primarios, biológicos, secundarios, espesadores y gasómetro. |
| 2026-05-21 | Normalización de líneas de leyenda en el JSON 3D | El tooltip mostraba códigos genéricos (`aguas`, `fangos`) que no permitían verificar las cinco líneas del visor. | `generate_digital_twin.py` clasifica cada elemento en líneas normalizadas; `web/app.js` muestra etiquetas legibles y el auditor verifica recuentos por línea. |
| 2026-05-22 | Leyenda interactiva y servidor con URL directa | La revisión visual por capas requería apagar/encender líneas y evitar adivinar el puerto disponible. | `web/index.html` y `web/app.js` convierten la leyenda en filtros por línea; `scripts/serve_web.py` busca puerto local libre e imprime `http://localhost:PUERTO/web/`. |
| 2026-05-22 | Migración del Gemelo Digital al contrato real MBR + fangos avanzados | El auditor contra `real.pdf` exigía sustituir el esquema simplificado por el proceso real de Silvouta. | `generate_digital_twin.py` modela MBR, reactores biológicos abiertos con agua transparente y aireadores, línea completa de fangos con hidrólisis térmica, escurridos, biogás, cogeneración y antorcha; la auditoría devuelve `CONFORME`. |
| 2026-05-22 | Conexión rigurosa de bombas y cota de aireadores | Análisis bibliográfico identificó aireadores sumergibles (que deben apoyar en el fondo, no flotar) y visualmente se auditaron bombas sin tubos de conexión. | Se conectaron explícitamente las succiones/impulsiones de bombeo y se bajó la cota Y de los aireadores al nivel de la solera del tanque biológico. |
| 2026-05-27 | Uso de fórmula de Darcy-Weisbach / Swamee-Jain en Excel | El modelo necesitaba cálculos de pérdidas de carga precisos para la línea piezometrica. | Se reemplazó la pestaña de bombeos por un cálculo detallado para 6 estaciones de bombeo basadas en `real.pdf`. |
| 2026-05-27 | Generación de Plano Base en formato CAD (.dxf) | Necesidad de pasar del gemelo digital conceptual a un plano editable topográficamente (Planta y Perfil) para Civil 3D. | Se creó el script `generate_dxf_plan.py` usando `ezdxf` para dibujar en coordenadas locales y mallas. |
| 2026-05-27 | Inclusión forzada de Perfil Hidráulico de Fangos | Requisito explícito del profesor/usuario, a pesar de que el fango discurre presurizado y no suele dibujarse en perfil. | Se añadió un bloque de dibujo en el DXF con el perfil secuencial de equipos de fangos y sus bombas. |
| 2026-05-27 | Auditor reproducible del Excel paramétrico | La revisión manual detectó que `check_excel.py` confundía cachés de fórmula no recalculadas con resultados cero reales. | Se añadió `scripts/audit_excel_model.py`, que audita fórmulas y referencias internas sin depender de valores cacheados. |
| 2026-05-27 | Corrección de referencias de fórmulas del Excel | Varias fórmulas apuntaban a filas de encabezado, celdas vacías o a sí mismas en bases, fangos y bombeos. | Se corrigió `scripts/generate_edar_excel.py`, se regeneró `EDAR_Modelo_Parametrico.xlsx` y la auditoría ya no detecta referencias vacías. |
| 2026-05-27 | Plano 04 V4 como CAD paramétrico de ingeniería | El DXF anterior se percibía como conceptual y no como diseño de ingeniería. | `generate_dxf_plan.py` ahora genera `plano_04_v4_ingenieria.dxf` con capas por disciplina, cotas, equipos etiquetados, tablas de parámetros, cuadros de tuberías/bombeos, perfiles y detalle MBR; `audit_dxf_plan.py` valida densidad mínima. |
| 2026-05-27 | Formulario de cálculo dentro del informe Word | El Anejo 2 debía mostrar explícitamente las fórmulas usadas en cada caso, no solo remitir al Excel. | `ESQUELETO_INFORME.md` y `ESQUELETO_INFORME.docx` incluyen 7 tablas de fórmulas; `scripts/generate_word_informe.py` queda como generador reproducible del Word. |
| 2026-05-27 | Diagrama de flujo en SVG editable | El usuario necesita un diagrama tipo proceso, como el ejemplo aportado, pero para el proyecto y editable en Inkscape. | Se creó `diagrama_flujo_edar_silvouta.svg` con capas de agua, fangos, biogás y retornos; `scripts/generate_process_flow_svg.py` calcula y rotula los datos principales del modelo. |
| 2026-05-27 | Excel separado por entradas, resumen y cálculos por línea | La revisión del usuario pidió una hoja de datos introducidos, hojas independientes de cálculo y un resumen de resultados siguiendo línea de aguas, fangos y otras líneas importantes. | `generate_edar_excel.py` regenera `EDAR_Modelo_Parametrico.xlsx` con 10 hojas ordenadas: datos, resumen, agua, fangos/biogás/deshidratación y bombeos. Las fórmulas quedan trazables y con cachés numéricas calculadas. |
| 2026-05-27 | Incorporación de criterios de Temas 1-6 al Excel | El usuario pidió revisar `Tema 1.pdf` a `Tema 5 y 6.pdf` para detectar datos faltantes del Excel. | Se añadió `02_Datos_Faltantes` y se incorporaron caudales, subproductos de pretratamiento, lamelar, bombeo primario y aireación; `REVISION_TEMAS_EXCEL.md` documenta qué se incorporó y qué sigue pendiente. |
| 2026-05-27 | Georreferenciacion e Inversion de Flujo EDAR | El usuario indico que el flujo real es de Oriente a Poniente y aporto los DEM MDT02. | Se creo plano_05_georeferenciado_topografia.dxf extrayendo curvas de nivel del TIF e invirtiendo el eje X del layout. |
| 2026-06-03 | Alcance academico simplificado para informe y Excel | La consigna y los detalles de evaluacion muestran que el profesor pide predimensionamiento defendible, no replicar el proyecto real completo. | Se creo `PLAN_ANALISIS_SIMPLIFICADO.md`; la solucion base queda centrada en linea de agua con MBR, linea de fangos con digestion anaerobia y deshidratacion, y linea de gas con biogas, energia y antorcha. La hidrolisis termica y otros bloques avanzados del proyecto real se trataran como referencia/mejora, no como nucleo de calculo. |
| 2026-06-03 | Extraccion trazable de fuentes de `manual/proyecto_real` | `TRABAJO DESGLOSADO.pdf` y `DETALLES EVALUACION Y EXAMEN.docx` son fuentes directas de evaluacion y debian estar disponibles para busqueda y trabajo con IA. | Se generaron TXT derivados en `manual/txt/` para los PDF principales de `manual/proyecto_real` y para el DOCX de evaluacion mediante extraccion zip/xml. |
| 2026-06-03 | Incorporacion de linea de olores al alcance simplificado | El usuario confirmo que debe agregarse linea de olores y la consigna la considera contenido valorable. | `PLAN_ANALISIS_SIMPLIFICADO.md` pasa a tratar olores como cuarta linea funcional, con focos, captacion, caudal de aire, desodorizacion y descarga. El Excel simplificado debera incluir hoja `35_Linea_Olores`. |
| 2026-06-07 | Implantacion condicionada por capas GIS de `1gis/Carga` | El usuario confirmo que `Zona` es limite fijo, que `Aliviadero Actual` es la entrada por gravedad, que el vertido debe ir al extremo izquierdo del meandro occidental del rio y que la gestion/transporte de fangos debe quedar cerca de la calle. | La propuesta de implantacion no debe ampliar la parcela; debe dibujar una conexion exterior desde el aliviadero, organizar la linea de agua de este a oeste, reservar descarga al meandro occidental y situar deshidratacion/silo de fangos junto al acceso viario. |
| 2026-06-07 | Base futura oficial para dimensionar la EDAR completa | El usuario pidió decidir la EDAR a partir del caudal en m3/d, afluente y efluente. El Anejo 1 contiene una base contractual completa para situación futura: Qmedio 64.800 m3/d, concentraciones de entrada y objetivos de vertido. | El Excel simplificado y la memoria se reconstruirán sobre la situación futura del Anejo 1. `EDAR_Modelo_Parametrico_v1.xlsx` se usará como referencia de valores oficiales, pero no como base cerrada sin auditar; `v0` queda como referencia académica previa. |
| 2026-06-07 | Modelo simplificado con huellas de equipos como base de implantacion | El usuario pidio generar la EDAR completa recordando los tamanos de los equipos y el espacio a escoger. | Se creo `scripts/generate_edar_simplified.py` y `2planilla/EDAR_Modelo_Simplificado_v0.xlsx`; la implantacion futura debera usar la hoja `60_Equipos_Espacios` y no ubicar equipos sin comprobar su huella, margen de servicio, viales y reserva. |
| 2026-06-07 | Gemelo Virtual migrado al contrato simplificado + GIS | El gemelo 3D anterior seguia el contrato avanzado del PDF real; el proyecto actual requiere representar la EDAR simplificada dimensionada con el Excel nuevo y las condiciones GIS indicadas por el usuario. | `scripts/generate_digital_twin.py` ahora genera `web/data/geometry.json` con aliviadero exterior, parcela, calle, rio/meandro occidental, agua, fangos, gas/energia y olores. `scripts/audit_digital_twin_lines.py` audita contra `EDAR_Modelo_Simplificado_v0.xlsx` y criterios GIS 2026-06-07; `web/index.html` y `web/app.js` incorporan la linea de olores a la leyenda. |
| 2026-06-07 | Diagrama de flujo base alineado con el modelo simplificado | El diagrama SVG anterior usaba datos del modelo parametrico previo y podia confundir la defensa del proyecto actual. | `scripts/generate_process_flow_svg.py` genera `0diagrama/diagrama_flujo_edar_silvouta_simplificado_v1.svg` con Qmedio 64.800 m3/d, BNR, MBR, FeCl3, fangos, biogas/energia, olores, retornos y notas de implantacion. |
| 2026-06-07 | Gemelo Virtual GIS como base visual, no rectangulo conceptual | El usuario detecto que el 3D no mostraba topografia, rio, poste electrico, calle ni punto de carga, y que habia equipos visualmente solapados. | `generate_digital_twin.py` pasa a leer `1gis/Carga` directamente: DEM, curvas, `Zona`, `Calle`, `rio` e `importantes`. Se añaden etiquetas visibles, terreno, poste electrico, punto de carga de fangos, camion, vertido occidental y auditoria de solapes AABB en huellas principales. |
| 2026-06-07 | Paquete reproducible de plantas y perfiles por linea funcional | El usuario pidio perfiles y plantas de linea de agua, fangos, gases y olores en DXF/PDF, con calculos Excel, informe, topografia, rio, calle, poste electrico, punto de vertido y punto de carga de fangos. | Se crea `scripts/generate_profiles_plans.py`, que genera `4planos/EDAR_Perfiles_Plantas_v0.dxf`, `4planos/EDAR_Perfiles_Plantas_v0.pdf`, `2planilla/EDAR_Perfiles_Plantas_v0.xlsx`, informe Markdown/PDF y manifiesto. Las plantas quedan en UTM EPSG:25829 y los perfiles se calculan sobre `TopoDEM.tif` como rasantes preliminares de anteproyecto. |
| 2026-06-08 | Acople DXF-gemelo como condicion de validacion | El usuario recalco que los DXF deben estar adecuados al gemelo digital. La primera version tenia algunas huellas agregadas en planta aunque el gemelo mostraba unidades individualizadas. | `generate_profiles_plans.py` dibuja BNR, MBR y depositos tampon como unidades separadas alineadas al gemelo. Se añade `scripts/audit_profiles_plans.py`, que valida capas, rutas, perfiles, Excel/PDF/manifiesto y coincidencia de nodos/equipos del DXF con anclajes de `web/data/geometry.json`. |
| 2026-06-08 | Transformacion visual independiente para el visor web | El usuario detecto que el Gemelo Digital se leia invertido frente a las imagenes GIS de `1gis/images`, aunque `geometry.json` y los DXF conservaban coordenadas GIS coherentes. | `web/app.js` mantiene los datos fuente en X=Este y Z=Norte, pero renderiza con `X_visual = -X_GIS` para que la vista Planta de Three.js coincida con QGIS. Los DXF y manifiestos no se invierten y siguen siendo la referencia georreferenciada. |
| 2026-06-09 | Cache-bust y Planta superior fija para el visor | La inversion seguia apareciendo al usuario, probablemente por una combinacion de script cacheado y transicion oblicua de camara en la vista `Planta`. | `web/index.html` carga `app.js?v=20260609-orientacion-final`, `web/app.js` versiona `geometry.json`, reemplaza la animacion por presets instantaneos y bloquea rotacion en `Planta`; `scripts/serve_web.py` sirve con no-cache. |
| 2026-06-09 | Integración de bases de diseño, justificación MBR y arquitectura del Gemelo en la Memoria | El usuario solicitó registrar las bases de diseño de calidad de agua, la justificación de MBR vs. convencional en la parcela útil GIS, y las librerías necesarias para el gemelo digital real. | Se modificó `3informe/ESQUELETO_INFORME_v0.md` insertando las cargas y concentraciones calculadas, detallando la viabilidad del MBR por restricciones espaciales de la parcela (3.73 ha) y proponiendo la arquitectura de librerías open-source (wwtpsim, ezdxf, epanet, etc.). |
| 2026-06-09 | Trazados ortogonales, codos hidráulicos y gráficos de simulación | El usuario solicitó eliminar intersecciones de tuberías con reactores, añadir pérdidas menores por codos de 90° con elevaciones por bombeo y mostrar gráficos temporales interactivos de simulación. | Se modificaron los trazados de tuberías en el DXF y el gemelo 3D para que discurran por pasillos, se incorporó Darcy-Weisbach y Swamee-Jain con pérdidas por codos y potencia de bombeo en el Excel y la simulación, y se integró un panel de 4 gráficos en tiempo real con Chart.js en la web. Ambas auditorías devuelven CONFORME. |
| 2026-06-09 | Solución al estiramiento/redimensionamiento infinito de gráficos | Los canvas de Chart.js sufrían un bucle de redimensionamiento (resize loop) al renderizarse en una caja flexbox con barras de scroll. | Se encapsularon los lienzos canvas en contenedores relativos `.chart-wrapper` con altura fija de 110px y `overflow: hidden`, configurando los canvas como elementos absolutos para cortar la realimentación del bucle. |
| 2026-06-10 | Sincronización del diseño hidráulico y trazado físico de tuberías | El usuario indicó que las cotas, codos y bombas de la EDAR deben responder estrictamente a los cálculos hidráulicos realizados. | Se actualizaron las longitudes y codos reales del trazado 3D en el script del Excel (B1: 70m, 3 codos; B5: 205m, 5 codos) recalculando pérdidas manométricas y potencias de motores. Se resolvieron los tramos de tuberías duplicados y se verificó conformidad en ambas auditorías. |
| 2026-06-10 | Redacción y unificación de Anejos 1-6 | Cumplir estrictamente con el esqueleto académico exigido y formalizar las secciones ausentes (Anejo 3, 5 y 6). | Creación de [MEMORIA_Y_ANEJOS_v1.md](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.md) conteniendo la justificación MBR, el predimensionamiento biológico/MBR y las secciones de energía/olores. |
| 2026-06-10 | Vinculación del Word a cálculos paramétricos de Python | Asegurar la consistencia numérica absoluta entre el libro Excel y la memoria técnica escrita. | Modificación de [generate_word_informe.py](file:///d:/projects/DSTA/scripts/generate_word_informe.py) para inyectar dinámicamente las variables de simulación en las tablas y textos del informe. Generación de [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx). |
| 2026-06-10 | Editor de Layout 3D interactivo y persistencia | Permitir al usuario reubicar y ordenar los equipos y tuberías en la planta desde la web y persistir la configuración en geometry.json, así como aislar los perfiles de proceso en vistas ortogonales. | Integración de TransformControls de Three.js y panel flotante de coordenadas XYZ en index.html/app.js. El movimiento de unidades traslada de forma solidaria sus sub-elementos (mallas de agua, difusores, etiquetas) y calcula extremos locales para tuberías. Se añadió el método HTTP POST /save-layout en serve_web.py para persistencia directa. |
| 2026-06-11 | Uso exclusivo de GPS-X 8.0 full para el paquete de simulacion | El usuario confirmo que la licencia full corresponde a GPS-X 8.0 y que GPS-X 9.0 es limitada y debe ignorarse. | `scripts/generate_gpsx_package.py` apunta a `C:/GPS-X80`, el paquete queda en `gps_x/DSTA_Silvouta_MBR_GPSX8/` y la version 9.0 no se usa como fuente ni destino. |
| 2026-06-11 | Layout compatible `06mbr1` dentro del paquete GPS-X | GPS-X 8.0 puede conservar referencias internas al basename original de la plantilla en sidecars o binarios. | La ruta recomendada de apertura es `layout_compat_06mbr1/06mbr1.lyt`; la copia renombrada DSTA se mantiene para trazabilidad, pero la compatibilidad operativa prioriza el basename original. |
| 2026-06-12 | Revision de cumplimiento por lineas del proyecto | El usuario pidio asegurar si estaban todas las lineas y si el proyecto cumplia lo requerido. | Se registra que, como proyecto preliminar academico, estan cubiertas agua, fangos/lodos, gas/energia/biogas, olores, implantacion, planos/perfiles, Excel, memoria y defensa. La validacion funcional GPS-X queda separada del cumplimiento documental/estructural. |
| 2026-06-12 | Alcance real de biogas y olores en GPS-X 8.0 | Se reviso el paquete GPS-X y se confirmo que el digestor contiene salida/parametros de gas, pero no una linea completa de gas/energia ni olores. | GPS-X se usa para tren agregado de proceso con digestion y dato de biogas; la red completa de gas/energia y la linea de olores se mantienen en Excel, memoria, DXF/PDF, perfiles y gemelo digital. |
| 2026-06-12 | Biogas es requisito teorico del proyecto | Las fuentes de evaluacion y desglose piden linea de gas, aprovechamiento de biogases y justificacion de energia/potencia aprovechada del biogas de digestion. | La linea de gas/biogas es obligatoria en los entregables del proyecto; no debe eliminarse aunque GPS-X no la modele como tren energetico completo. |
| 2026-06-12 | Adopcion de Qpunta por coeficiente 1,30 | El usuario indico usar el caudal de Vero: 64.800*1,3 = 84.240 m3/d. El Anejo 1 conserva tambien una tabla redondeada de 86.400 m3/d. | El modelo usa 84.240 m3/d para dimensionamiento seco y conserva 86.400 m3/d como contraste trazable en hojas de caudales, trazabilidad y defensa. |
| 2026-06-12 | Integracion de trazabilidad y defensa rescatada de la planilla de Vero | La defensa necesita que cada ecuacion importante indique origen, fuente/PDF y hoja de uso. | `EDAR_Modelo_Simplificado_v0.xlsx` incorpora `05_Trazabilidad_Formulas_Fuente`, `06_Auditoria_Trazabilidad`, `73_Riesgos_Cautelas` y `95_Puente_GPSX`. |
| 2026-06-12 | MBR conservador por espacio y operacion N-1 | La parcela es escasa y el usuario pidio mantener una solucion defendible; Vero usaba un criterio mas conservador. | Se adopta MBR a 20 LMH, 8 trenes 7+1, 175.500 m2 de membrana y flujo N-1 de 22,86 LMH. La huella se mantiene como 4 canales dobles compactos. |
| 2026-06-12 | Separacion hidraulica de lluvia y biologico/MBR | Vero explicaba que el caudal de lluvia va a cabecera/primario y no al BMR/MBR completo; el usuario pidio implementarlo. | Se agrega tanque de tormenta de 3.645 m3, con 6 m utiles para reducir huella. El MBR se dimensiona con punta seca; la lluvia completa no entra al MBR. |
| 2026-06-12 | Criterio defendible de fosforo, lodos y biogas | El usuario pidio guiarse por lo mas defendible en biogas/lodos y no generar diferencias absurdas con GPS-X. | Se adopta EBPR 60% con FeCl3 40% de respaldo, rendimiento de fango biologico 0,65, fango quimico 5 kgMS/kgP y torta al 28%. Resultan digestores 9.117 m3 y biogas 6.799 m3/d. |
| 2026-06-12 | Bombas y perdidas como soporte GPS-X trazable externo | El usuario pidio indicar perdidas y bombas en GPS-X, pero la plantilla `06mbr1` no representa la red hidraulica completa ni sustituye el calculo Excel/DXF de cotas, codos y perfiles. | El paquete GPS-X incorpora `inputs/hydraulic_pumps_and_losses.csv`, filas B1/B5 en `gpsx_parameter_map.csv`, README, mapeo, checklist y resumen JSON. B1/B5 quedan documentadas para comparar energia/cotas; solo se convertiran en unidades GPS-X si se mapean manualmente bombas explicitas y se registra el cambio. |
| 2026-06-12 | Sintonizacion inicial del MBR GPS-X con volumen y purga de diseno | La primera corrida estacionaria en GUI uso el volumen MBR de plantilla (500 m3), produjo SRT bajo, MLSS irreal y no nitrifico, por lo que no representaba el Excel simplificado. | `scripts/generate_gpsx_package.py` inyecta en el layout `Cvmcon&o=20020`, `Cmemarea&o=175500`, `Ctankarea&o=3640`, modo MBR de volumen/calculo y purga inicial `Cqcon&p=417,083333` con `Cqmax&p=1251,25` por linea. Se debe reabrir el layout compatible y repetir la corrida. |
| 2026-06-12 | Perfil redox MBR 1 anoxico + 3 aerobicos en GPS-X | El usuario propuso usar el criterio de ejercicios previos: primera zona anoxica y las siguientes aerobicas. Frente a 2 anoxicas + 2 aerobicas, aumenta el volumen aerobico disponible y deberia favorecer la nitrificacion inicial. | El layout GPS-X queda con `1setpsol&o=0.0F2.0F2.0F2.0F` y `1fraircon&o=0.0F0.5F0.5F0.5F`. Tras correr, se debe revisar NH4, NO3/TN, MLSS, SRT y WAS; si nitrifica pero queda mucho nitrato, ajustar recirculacion interna o volumen anoxico. |
| 2026-06-12 | Biomasa inicial defendible en GPS-X antes de modificar el resto | La corrida dinamica con el MBR corregido seguia mostrando MLSS del orden de 150 mg/L y NH4 alto, coherente con un arranque sin biomasa. El usuario pidio usar bibliografia existente para defender la semilla y probar primero GPS-X. | El layout compatible incluye en `!INZ` una biomasa inicial Mantis2 con MLSS objetivo 7.000 mg/L y MLVSS 5.250 mg/L: `1ixbhl`, `1ixbail`, `1ixbaal`, `1ixil`, `1ixul`, `1ixsl`, `1ixiil` y solubles iniciales. La base bibliografica queda trazada a `BIO_MLSS`, manual de fangos activos y referencia BRM/MBR con SSLM 5,2-10 g/L. Excel/memoria no se modifican hasta validar la corrida. |
| 2026-06-13 | Recirculacion interna MBR Ri=3 aplicada dentro de GPS-X | Al comparar con `D:\projects\Asus\gps-x_9` y con `C:/GPS-X80/mantis2lib/mantimbr.con`, se confirmo que `1flowrate&o` es recirculacion interna, no aireacion. El modelo anterior estaba cargando ahi el aire preliminar, dejando Ri muy bajo. | `scripts/generate_gpsx_package.py` carga `1flowrate&o(1)=194.400 m3/d`, `from tank 4 -> to tank 1`, fracciones 0,25/0,25/0,25/0,25, y B5 como `Cintpumphead` + `Cintpumpheadloss`. Los outputs de efluente se amplian con NH4, P soluble y P total. Correr primero el layout Ri3 antes de tocar Excel/memoria. |
| 2026-06-13 | Mantener MBR GPS-X con 4 zonas internas y rotulos en español | La plantilla `06mbr1` usa el modelo `mantimbr.con`, que fija `n&o=4`; pasar a 5 zonas exige cambiar de arquitectura o plantilla. El usuario pidio entender y visualizar mejor el modelo. | El layout Ri3 queda con `Reactor MBR 4 zonas`, zona 1 anoxica y zonas 2-4 aerobicas. Los equipos y pestañas se renombran en español y los outputs quedan como `Afluente`, `Primario`, `Reactor MBR`, `Vertido`, `Perfiles MBR`, `Membranas` y `Calidad efluente`. |
| 2026-06-13 | Separar objetivos oficiales de salida de resultados GPS-X preliminares en una ficha Word | El usuario pidio un Word con calidad/caudal de entrada y salida exigidos por el proyecto, y aclaro que `Caso6_Jose_Espinoza.pdf`/`.docx` solo aporta ideas de configuracion del reactor. | Se crea `3informe/base_caudal_calidad_entrada_salida_v0.docx` desde `scripts/generate_base_caudal_calidad_docx.py`. La ficha usa Anejo 1 y Excel simplificado como fuentes oficiales, y deja Caso6 solo como idea externa: anoxico inicial, zonas aireadas, SRT y recirculacion interna. |
| 2026-06-13 | Incorporar area y pendiente del terreno a la ficha base del proyecto | El usuario pidio que el Word incluya, ademas de caudal/calidad de entrada y salida, el area y la pendiente del terreno. | `scripts/compute_terrain_stats.py` calcula superficie y pendientes desde `Zona.shp` y `TopoDEM.tif`; `base_caudal_calidad_entrada_salida_v0.docx` incluye area 37.306,95 m2, pendiente global 1,84% hacia S y pendiente local media 9,70%, dejando claro que es dato DEM de anteproyecto. |
| 2026-06-13 | Reconstrucción desde cero (Línea de Agua) en `gpsx8_2` | Corregir el error hidráulico de caudal cero en el efluente final aislando solo la línea de agua primero. | Se crearon archivos de layout limpio en `gpsx8_2` copiados del original `06mbr1` y se modificó `generate_water_line.py`. |
| 2026-06-13 | Reemplazo del Decantador Primario por FeCl3 (`metaladd`) | Evitar el decantador primario en la primera fase para asegurar disponibilidad de carbono para desnitrificación. | Se sustituyó el bloque del primario (`simple1d`) en la coordenada `15 18` por un bloque `metaladd` (dosis inicial 0) actuando como pasante. |
| 2026-06-13 | Conexión directa de WAS a sumidero de lodos | Mantener MBR sin puertos desconectados ("no hay puertos sin conectar en el MBR") y derivar el fango de purga. | Se enlazó el puerto 3 de MBR directamente al bloque `Sludge` en `19 16`, anulando los bloques de espesador, digestor y deshidratación temporalmente. |
| 2026-06-13 | Uso de Caudal Punta y Temperatura Crítica en la Línea de Agua | El usuario solicitó simular el peor caso de diseño: caudal punta de 84.240 m3/d y temperatura de 13 °C. | Se modificó `generate_water_line.py` y `audit_water_line.py` para parametrizar el afluente a 84.240 m3/d, la recirculación interna a 252.720 m3/d (Ri=3) y la temperatura global de simulación a 13 °C. La generación y auditoría concluyeron en CONFORME ESTRUCTURAL. |
| 2026-06-13 | Corrección de sintaxis y auditoría del layout en `gpsx8_2` | El compilador de GPS-X fallaba debido a un espacio en blanco antes de `Xtransfermethod` en el archivo `.lyt` y el auditor daba error por formato. | Se eliminó el espacio en blanco antes de `Xtransfermethod&o` en `generate_water_line.py` y se corrigió el string de búsqueda del flujo en `audit_water_line.py` (de `0.0;` a `0.0!;`). El layout ahora se autocompila correctamente en GPS-X y el auditor arroja resultado CONFORME ESTRUCTURAL. |
| 2026-06-13 | Mapeo de volumen MBR a modo Fixed Volume | El solver estacionario (Trim) de GPS-X no converge con control de nivel dinámico en volumen variable (Xmbrvol=2), dando caudal efluente 0.0 m3/d. | Se cambiaron `Xmbrcalc` y `Xmbrvol` a `1` en `generate_water_line.py` y `generate_gpsx_package.py`. Esto desactiva el lazo de control de nivel dinámico y fija el volumen constante, logrando que el balance hidráulico fluya correctamente y el simulador muestre el caudal real del efluente. |
| 2026-06-13 | Corrección de incompatibilidad (vectores MBR con sufijo F) | Al abrir el archivo en la GUI saltaba un error de incompatibilidad que causaba que GPS-X reseteara el reactor MBR a los valores por defecto (500 m3). | Se identificó que GPS-X requiere que todos los vectores finalicen con `F` (ej: `0.25F0.25F0.25F0.25F!`). Se corrigió el formateador y los auditores en `generate_water_line.py`, `generate_gpsx_package.py` y los auditores correspondientes. El error de incompatibilidad y el reseteo de parámetros al abrir el layout han sido resueltos. |
| 2026-06-13 | Corrección de sintaxis en el archivo .lyt por modificadores inválidos | Los usuarios experimentaban errores de compilación `qcon- = 10.0;` y fallos al cargar el bloque `simple1d` y MBR. | Se identificó que el script usaba el modificador inválido `&s` en lugar de `&p` para parámetros de purga de bombeo, además de atributos no oficiales en MBR. Se limpiaron `generate_water_line.py` y `generate_gpsx_package.py`. |
| 2026-06-13 | Configuración de 5 zonas (1 anaerobia + 4 aeróbicas) en GPS-X | El usuario solicitó cambiar a 5 reactores y establecer una recirculación desde la zona aeróbica 5 a la anaerobia 1. | El bloque `mantimbr.con` está limitado internamente a 4 zonas (`n&o=4`). Se decide que para lograr 5 zonas, se debe incorporar de manera gráfica un tanque anaerobio separado (CSTR/Plug-flow) antes del MBR y conectar la recirculación de forma externa en la interfaz de GPS-X. |
| 2026-06-13 | Variante reproducible `gpsx8_2_tuned_5zone` para resolver NH4 alto | La GUI permitió guardar `Pn&o=5` en `gpsx8_2`, pero el NH4 seguía alto. La captura muestra hidráulica correcta y apunta a SRT baja/purga excesiva, NH4 afluente no explícito y falta de volumen aeróbico, no a un problema primario de KLa. | Se crea una variante separada con Qmedio 64.800 m3/d, `Csnh&o=25`, 5 zonas 20/20/20/20/20, OD 0/2/2/2/2, recirculación interna R5->R1 a Ri=4Q, purga fija `Cqcon&p=800,8 m3/d` para SRT ~25 d y controlador MLSS de purga desactivado. La decisión previa de 5 zonas imposibles queda como cautela para el paquete formal, pero la variante GUI se acepta como prueba funcional. |
| 2026-06-13 | Mantener controlador de OD antes de probar KLa | En `mantimbr.con`, `Xtransfermethod&o=3` significa controlador de OD; los parámetros `klacon&o` solo gobiernan si `Xtransfermethod&o=2`. | No se sube KLa como primera acción. La variante mantiene `Xtransfermethod&o=3`, carga KLa/aire solo como respaldo y la validación funcional debe revisar si el OD real llega a 2 mg/L en zonas 2-5 antes de cambiar de método de oxígeno. |
| 2026-06-13 | Subir SRT objetivo de la variante 5 zonas a 30 d | La revisión teórica de `manual/best` indica que la nitrificación depende fuertemente del SRT, que a baja temperatura conviene margen de seguridad, y que la aireación extendida/MBR puede operar alrededor de 20-30 d. Con NH4 alto y OD en consigna, la primera acción defendible es proteger biomasa nitrificante antes que tocar FeCl3 o línea de fangos. | `generate_water_line.py` calcula ahora WAS con volumen efectivo MBR `Vaerobio + R/(R+1)*Vanoxico = 19.219,2 m3`; queda `Cqcon&p=640,64 m3/d` para SRT ~30 d y `Cqmax&p=1067,73 m3/d` como referencia de SRT ~18 d. `audit_water_line.py` fue actualizado y la variante `gpsx8_2_tuned_5zone` pasa auditoría estructural. |
| 2026-06-13 | Sustituir ruta operativa de 5 zonas por variante estable 4 zonas | El usuario reportó que el archivo tardaba mucho en abrir. La revisión de `C:/GPS-X80/mantis2lib/mantimbr.con` confirma que el MBR nativo usa `n&o=4`; forzar `Pn&o=5` y vectores de 5 posiciones puede ralentizar o desestabilizar la carga. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_fast4zone`, con 4 zonas nativas, OD 0/2/2/2, Ri=4Q desde zona 4 a zona 1, SRT 30 d, `Cqcon&p=633,966667 m3/d` y sin `Pn&o=5`. `gpsx8_2_tuned_5zone` queda como experimento histórico, no como ruta recomendada. |
| 2026-06-13 | Aislar el reactor MBR de 5 zonas antes de agregar unidades auxiliares | El usuario indico que la variante de 4 zonas tampoco era la solucion y pidio volver a 5 reactores, dejando solo el reactor hasta conseguir valores biologicos razonables. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_reactor_only_5zone`: solo Afluente, Reactor MBR 5 zonas, Vertido y Fango WAS. Se retiran temporalmente FeCl3, UV, espesador, digestor, deshidratacion y retornos. Se conserva Qmedio 64.800 m3/d, 13 C, OD 0/2/2/2/2, Ri=4Q desde R5 a R1, SRT 30 d y WAS 640,64 m3/d. |
| 2026-06-13 | Cerrar `Pn&o=5` interno como invalido y pasar a arquitectura externa 1+4 | La GUI de GPS-X queda pegada y Java muestra `ArrayIndexOutOfBoundsException: 5`, confirmando que el bloque MBR nativo no soporta un quinto compartimento interno. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_external5_mbr4_reactor_only`: tanque anoxico externo `mantis2` de 5.005 m3 + MBR nativo de 4 zonas aerobicas de 15.015 m3. Se elimina `Pn&o=5`, se conserva WAS 633,966667 m3/d para SRT 30 d y se documenta Ri=4Q R5->R1 como ajuste GUI pendiente al confirmar puerto de licor mezcla. |
| 2026-06-14 | Preparación de EDAR Silvouta final en GPS-X | El usuario indicó que layout_compat_06mbr1_Ri3 es el único que funciona bien. | Se copió ese layout a layout_EDAR_Silvouta renombrando los archivos a EDAR_Silvouta.* para tener el diseño final listo para correr. |
| 2026-06-14 | Configuracion GUI de la Recirculacion Interna (MLR) en el MBR | El usuario experimentaba flujo cero al intentar retornar lodos desde el reactor de membranas a la cabecera mediante tuberias fisicas o tablas internas fallidas. | Se decide priorizar la tabla nativa Internal Flow Distribution del bloque MBR, tal como dicta el manual oficial, y no fabricar bucles fisicos externos con bombas o splitters, para mantener la limpieza del layout. Se instruyo al usuario para solucionar los errores de sintaxis (qrecirculacion) introducidos en los campos de control. |
| 2026-06-14 | Crear `layout_EDAR_Silvouta_v2` con basename compatible `06mbr1` | El layout manual `layout_EDAR_Silvouta` mezclaba basename renombrado, no incluia `.prx` y forzaba `Pn&o=5`/vectores de 5 zonas en un MBR nativo limitado a 4 zonas. El usuario confirmo Q adoptado 84.240 m3/d. | La v2 se genera desde `layout_compat_06mbr1`, se abre como `layout_EDAR_Silvouta_v2/06mbr1.lyt`, mantiene 4 zonas internas, Q=84.240 m3/d, T=13 C, Ri=3 y WAS fijo preliminar para SRT aproximada de 20 d. Se documenta el diagnostico de nitrogeno y se audita con `audit_layout_edar_silvouta_v2.py`. |
| 2026-06-14 | Usar `Membrane Bioreactor (MBR)`/`06mbr1` y no otros MBR para Silvouta v3 | La paleta GPS-X contiene `Membrane Bioreactor (MBR)`, `Completely-Mixed MBR` y `Anaerobic MBR`; el usuario pidio usar solo el que sirva mejor. Para bajar N se necesitan zonas redox y recirculacion interna. | Se elige `Membrane Bioreactor (MBR)`/plantilla `06mbr1`. Se descarta `Completely-Mixed MBR` para nitrogeno por no representar zonas, y se descarta `Anaerobic MBR` por no aplicar a nitrificacion municipal. La v3 mantiene 4 zonas, KLa explicito, Ri=3,5 y retornos de fangos cerrados a cabecera. |
| 2026-06-14 | Reincorporar FeCl3 como variante separada `layout_EDAR_Silvouta_v4_quimica` | El layout manual anterior contenia un `metaladd` valido para FeCl3, pero tambien arrastraba `Pn&o=5` y vectores de 5 zonas inestables. El control quimico sirve para fosforo y no debe confundirse con la causa del NH4 alto. | La v4 se genera desde la base v3 y agrega `metaladd` entre `peff` y MBR (`Primario -> FeCl3 -> MBR`), con `Xchemical=4`, `Xdosmode=2` y dosis activa 15 gFe/m3. Se documenta que 15 gFe/m3 es un caso fuerte; el Excel con EBPR 60% equivale a unos 5 gFe/m3, por lo que la calibracion final debe comparar 0/5/15 gFe/m3 tras estabilizar la biologia. |
| 2026-06-14 | Adopción de configuración GPS-X punta funcional como base de sincronización | La ultima corrida funcional en GUI del usuario valida el escenario punta con Q=84.240 m3/d y efluente conforme; era necesario separar la fuente final de las variantes historicas de depuracion. | Se adopta como layout funcional final `D:\projects\DSTA\gps_x\DSTA_Silvouta_MBR_GPSX8\layout_EDAR_Silvouta_Final` (`PlantaEDAR.lyt`). Se adopta MBR nativo GPS-X 8.0 de 4 zonas y se descarta `Pn&o=5` interno. La recirculacion interna queda 4->1 = 497.220 m3/d en `Internal Flow Distribution`, WAS = 600 m3/d, FeCl3 = 15 gFe/m3 y SRT = 30,76 d. El escenario punta cumple objetivos de efluente y Excel/CAD/SVG/3D/informe deberan sincronizarse con estos resultados. Los layouts anteriores no se borran; se mantienen como historial tecnico hasta que el usuario autorice archivado o limpieza. |
| 2026-06-14 | Creación de Excel Modular por equipos v1_COMPLETO | Necesidad de un predimensionamiento estático trazable por unidad de equipo, separado de la simulación funcional de GPS-X. | Se creó `EDAR_Modelo_Modular_Tipico_GPSX_v1_COMPLETO.xlsx` con 17 hojas enlazadas. Sirve como base para el diseño civil y electromecánico. |
| 2026-06-14 | Implantación GIS-First (EPSG:25829) | Asegurar que la ubicación de equipos responda a la topografía real y límites legales antes de generar planos CAD. | Se generaron capas en `1gis/EDAR_Modular_GPSX_v1/` (GPKG, SHP, KML) con huellas de equipos, cotas y pendientes extraídas del DEM. |
| 2026-06-14 | Word como Centro de Información del Proyecto | Consolidar objetivos, resultados validados y compendio de ecuaciones en un único documento de referencia técnica. | Se generó `base_caudal_calidad_entrada_salida_v1_centro_info.docx` incluyendo el compendio de fórmulas de diseño estático. |
| 2026-06-14 | Crear `EDAR_Modelo_Simplificado_v1_GPSX.xlsx` como puente Excel entre GPS-X y entregables | El v0 debia mantenerse intacto y los resultados funcionales GPS-X necesitaban una capa Excel trazable antes de actualizar CAD, SVG, visor 3D o informe. | Se crea `2planilla/EDAR_Modelo_Simplificado_v1_GPSX.xlsx` desde v0 mediante `scripts/update_excel_gpsx_v1.py`. La v1 incorpora fuente de datos, resumen de cumplimiento, linea de agua GPS-X, fangos GPS-X, biogas pendiente de reconciliacion, olores preliminares, defensa y puente de entregables. La v1 es el puente de trabajo para las fases siguientes; v0 queda como respaldo intacto y no se actualizan aun CAD/SVG/3D/informe. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v1.xlsx` para defensa academica | El Excel maestro GPS-X v1 es util como puente tecnico, pero para defensa conviene una version reducida, legible y centrada en etapas, variables, ecuaciones, resultados y preguntas esperables. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v1.xlsx` mediante `scripts/create_excel_lite_gpsx_v1.py`, con solo 8 hojas y validacion contra `_control_proyecto/fuente_oficial_gpsx.yaml`. No sustituye al modelo tecnico completo, no modifica `EDAR_Modelo_Simplificado_v1_GPSX.xlsx`, mantiene biogas como pendiente de reconciliacion y olores como calculo preliminar externo a GPS-X. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v2_formulas.xlsx` con formulas activas | La v1 lite era legible para defensa, pero algunas salidas derivadas estaban pegadas como valores y no como calculos auditables. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v2_formulas.xlsx` desde la v1 mediante `scripts/create_excel_lite_gpsx_v2_formulas.py`. La v2 mantiene las 8 hojas, agrega referencias a variables base, `FORMULATEXT`, formulas de cumplimiento, fangos/camiones, biogas preparado y olores por foco. No modifica la v1 original ni actualiza CAD, SVG, visor 3D, Word/PDF o informe. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v3_ecuaciones.xlsx` con criterio estricto de ecuaciones | La revision del usuario exige que solo los datos iniciales queden como valores y que todo resultado derivado se obtenga por ecuacion visible, dejando faltantes editables para completar calculos al introducir datos. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v3_ecuaciones.xlsx` desde la v2 mediante `scripts/create_excel_lite_gpsx_v3_ecuaciones.py`. La v3 clasifica `02_Variables` por tipo de dato, convierte derivados a formulas activas con columnas de ecuacion/FORMULATEXT, deja biogas/eficiencia como entradas faltantes y valida que v1/v2 no cambien. No actualiza CAD, SVG, visor 3D, Word/PDF ni informe. |
| 2026-06-14 | Crear informe de alcance de implantacion y matriz de equipos antes del Excel modular | El usuario pidio decidir que equipos se pueden/deben incluir, separando lo minimo, lo GPS-X, lo externo a GPS-X, lo recomendable, lo opcional y lo que no conviene modelar para defensa academica. | Se crea `3informe/informe_alcance_implantacion_y_equipos_v1.md` y `_control_proyecto/matriz_equipos_implantacion.csv`. El informe fija el minimo defendible, el maximo razonable, la evaluacion espacial y las hojas recomendadas para un Excel modular de equipos, sin modificar Excel, CAD/DXF, SVG/Inkscape, visor 3D, Word/PDF, informe final ni layouts GPS-X. |
| 2026-06-14 | Actualizar el diagrama SVG/Inkscape con la fuente GPS-X final | El diagrama de flujo seguia representando el modelo simplificado previo y la Fase E del plan pedia sincronizar SVG/Inkscape tras validar GPS-X. | `scripts/generate_process_flow_svg.py` pasa a leer `_control_proyecto/diccionario_variables_edar.csv` y regenera `0diagrama/diagrama_flujo_edar_silvouta_simplificado_v1.svg` con Qpunta 84.240 m3/d, MBR 4 zonas, recirculacion 4->1, resultados de efluente, fangos/camiones y pendientes de biogas/olores. No actualiza CAD/DXF, visor 3D ni informe final. |
| 2026-06-15 | Preparar copia GPS-X outputs plus sin editar archivos internos automaticamente | El usuario pidio extraer variables faltantes y preparar displays/plots, pero el formato `.lyt`/`.sim` de GPS-X es propietario y una reescritura textual puede romper el layout funcional validado. | Se crea/verifica `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS`, se genera `outputs_extraidos_gpsx_plus/` con inventario de variables, outputs encontrados, pendientes, diccionario de displays/plots, guia manual y reporte; se registra la copia en `_control_proyecto/registro_copia_gpsx_outputs_plus.md`. Los displays/plots D01-D15 y P01-P10 se deben crear/exportar desde la GUI GPS-X sobre la copia. |
| 2026-06-15 | Usar `doci_Eflu` como enlace FeCl3 -> MBR en la copia OUTPUTS_PLUS | GPS-X guardo el enlace de salida de FeCl3 con un nombre distinto, pero el MBR conservaba `Spropvar=fecl3`, provocando el error de compilacion `qfecl3` indefinido. El usuario pidio conservar el nombre `doci_Eflu`. | `scripts/repair_gpsx_outputs_plus_qfecl3.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: salida FeCl3, entrada MBR y `Spropvar` quedan en `doci_Eflu`; se restaura Qpunta 84.240 m3/d y se genera respaldo/reporte. El layout final original no se modifica. |
| 2026-06-15 | Usar `MBR_Rebose` como nombre del rebose del MBR en la copia OUTPUTS_PLUS | El usuario renombro el rebose del MBR a `MBR_Rebose`, pero GPS-X mantenia una referencia interna antigua `Scvarp=xmbrof`, provocando el error de compilacion `xmbrof` indefinido. | `scripts/repair_gpsx_outputs_plus_mbr_rebose.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: el puerto se conserva como `MBR_Rebose` y el controlador queda como `Scvarp=xMBR_Rebose`. Se genera respaldo/reporte y el layout final original no se modifica. |
| 2026-06-17 | Adoptar `OUTPUTS_PLUS/max` como fuente documental gobernante | El usuario aclaro que la discrepancia se debe a que el layout quedo cerrado con promedio mientras se cargaban/exportaban maximo y promedio, y que todos los entregables deben usar caudal maximo. | `_control_proyecto/fuente_funcional_outputs_plus_max.yaml/json` fija `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/max/plantaedar.xls` como fuente para Excel, diagrama, GIS y documentos. `Promedio/` queda como antecedente no gobernante. |
| 2026-06-17 | Separar Excel completo de origen y Excel simplificado gobernante | La entrega necesita trazabilidad de ecuaciones/fuentes y, a la vez, un libro simple con entradas, formato y resumen que gobierne el resto de documentos. | Se crean `EDAR_Modelo_OUTPUTS_PLUS_ORIGEN_v1.xlsx` y `EDAR_Modelo_OUTPUTS_PLUS_Simplificado_v1.xlsx`. El primero conserva origen, hashes, tablas GPS-X y ecuaciones; el segundo gobierna memoria, diagrama, GIS y futuras actualizaciones de CAD/visor. |
| 2026-06-17 | Regenerar diagrama y poligonos GIS desde el Excel OUTPUTS_PLUS max | Las instalaciones adicionales pedidas por el informe no existen todas como unidades dinamicas en GPS-X y debian incorporarse al flujo documental y espacial. | Se genera `diagrama_flujo_edar_silvouta_outputs_plus_v1.svg` y `1gis/EDAR_Modular_GPSX_v2/` con 29 huellas, 0 equipos fuera de `Zona` y 0 solapes; CAD/DXF, visor 3D e informe quedan como siguientes derivados del Excel simplificado. |
| 2026-06-17 | Incorporar alturas TopoDEM en `equipos_edar` | El usuario pidio que los poligonos de equipos incluyan altura maxima, minima y promedio desde el raster `TopoDEM`. | `equipos_edar.gpkg` incorpora `altura_min_m`, `altura_max_m` y `altura_prom_m` por poligono. Si el SHP original esta bloqueado por QGIS, se entrega `equipos_edar_alturas.shp` con alias compatibles `alt_min_m`, `alt_max_m` y `alt_prom_m`. |
| 2026-06-17 | Reparar variables internas de OUTPUTS_PLUS en copia de dos trenes | GPS-X guardo streams con sufijos `doci_Eflu1/2` y `MBR_Rebose1/2`, pero el primer MBR conservaba referencias internas sin sufijo que podian compilar como variables indefinidas. | `scripts/repair_gpsx_outputs_plus_split_trains.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: `Spropvar=doci_Eflu1`, `Scvarp=xMBR_Rebose1` y `Cqcon&o=84240.0`. Se genera respaldo/reporte y el layout final original no se modifica. |
| 2026-06-18 | Completar el MBR 2 de OUTPUTS_PLUS sin cambiar la logica activo/reserva | El usuario aclaro que el reparto `Cfr&in&to&o1=1.0` es intencional porque una linea puede quedar en mantenimiento y debe activarse la otra; el fallo estaba en que el MBR 2 no tenia los parametros internos completos del MBR 1. | `scripts/repair_gpsx_outputs_plus_line2_mbr.py` copia al MBR 2 area de membranas, control MLSS/purga, inicializacion biologica y DVA del MBR 1, adaptando `Spropvar=doci_Eflu2` y `Scvarp=xMBR_Rebose2`. No cambia el divisor de caudal y no modifica `layout_EDAR_Silvouta_Final`. |
| 2026-06-18 | Crear carpeta `apoyo/` para trabajo auxiliar con Gemini | El usuario pidio ahorrar tokens de OpenAI delegando tareas pesadas a Gemini cuando sea conveniente. | `apoyo/` queda como zona no oficial de intercambio: `pedidos_gemini/` contiene instrucciones especificas, `salidas_gemini/` contiene respuestas, y `INSTRUCCIONES_GEMINI.md` fija que Gemini no modifica fuentes oficiales ni sustituye los Markdown de control. |
| 2026-06-19 | Reparación de tuberías candidatas v2 | Corregir conexiones para que conecten a vértices exteriores sin penetrar polígonos, y solucionar NoData del DEM en Z | Se generaron conexiones ortogonales en GPKG/SHP conectadas a esquinas, se calcularon codos, longitudes 3D y bombeo preliminar con fallback robusto a cota de equipos. |
| 2026-06-19 | No integrar el candidato Qmedio v2 hasta reparar auditoria Codex | La auditoria independiente detecto que v2 aun tiene tramos diagonales en conexiones exteriores, intersecciones con poligonos como segmentos, codos subestimados y una diferencia de superficie Excel/GIS por `A11_VERTIDO`. | Se crea `apoyo/pedidos_gemini/pedido_20260619_reparar_candidato_qmedio_v3_post_auditoria_codex.md`; la proxima salida debe ser una carpeta candidata v3 y no debe tocar archivos oficiales ni Markdown de control. |
| 2026-06-19 | Aceptar v3 como candidato GIS/SVG preferente, no integrado aun | La auditoria Codex de v3 confirma geometria de tuberias conforme: ortogonalidad, vertices, ausencia de intersecciones con poligonos, longitudes 3D y conteo de codos. Queda solo una cautela documental sobre el metodo de conteo de cruces XY entre lineas. | `candidato_20260619_qmedio_svg_gis_tuberias_v3` queda como candidato preferente para promocion oficial cuando el usuario lo apruebe; antes de usar la estadistica de cruces en memoria/planos se debe aclarar 27 pares Gemini frente a 33 pares Shapely/Codex con interseccion no vacia. |
| 2026-06-19 | Generar QGZ de revision antes de promocion oficial | El usuario quiere revisar visualmente el candidato v3 en QGIS antes de integrarlo en las carpetas oficiales. | Se genera `qgis_revision_candidato_qmedio_v3.qgz` dentro de la carpeta candidata, con rutas relativas y capas activadas de DEM, Zona, equipos, tuberias 3D y centroides. Este QGZ es de revision, no una promocion oficial. |
| 2026-06-19 | No integrar v3 tras revisión visual del usuario | El usuario observó que algunas tuberías salían del área de estudio de la meandro `Zona.shp`. | Se decidió rechazar el candidato v3 y generar una versión v4 que mantenga todas las geometrías al 100% dentro del polígono de Zona. |
| 2026-06-19 | Generar candidato v4 con restricciones de área estrictas y dos líneas | Asegurar que ninguna tubería salga de `Zona.shp`, documentar la configuración de doble línea (L1 activa, L2 reserva) en atributos GIS, y actualizar nombres en español en el SVG. | Se inyectó control estricto de límites en el ruteador (`route_pipes_v5.py`) y 10 atributos de doble línea en equipos, y se tradujeron etiquetas del SVG a español con acentos correctos. La auditoría final reporta 0 elementos fuera de Zona. |
| 2026-06-19 | División física en dos líneas paralelas en GIS (Candidato v5) | El usuario solicitó representar de forma fidedigna el doble tren paralelo de la planta en el GIS, tal como se muestra en el layout GPS-X8. | Se modificó el ruteador (`route_pipes_v6.py`) para dividir físicamente los polígonos de decantadores, reactores MBR y UV en unidades independientes y trazar tuberías paralelas independientes (L1 activa, L2 reserva). Se corrigió la interpolación DEM y se compiló el proyecto QGIS de revisión v5. |
| 2026-06-19 | Promoción de la Alternativa Paralela (Candidato v5) y Compilación de Memoria Word | Establecer el trazado del Candidato v5 como versión oficial georreferenciada y asegurar la consistencia del informe Word. | Se ejecutó `promote_candidate_v5.py` para promover el Candidato v5 (capas GIS oficiales en `1gis/EDAR_Modular_GPSX_v2`, diagramas SVG y QGIS de revisión). Se compiló la memoria oficial [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx) con la justificación del reactor MBR optimizado a $5,5\text{ m}$ de profundidad, el cual reduce la huella a $4.550\text{ m}^2$ por reactor y garantiza contención total con cero solapes. |
| 2026-06-19 | Reparar solo la errata de superficie MBR en el Excel Qmedio oficial | La revision conjunta GPS-X8/Excel mostro que `07_MBR_activo!D11 = 3640 m2` es el area del tanque final por reactor MBR, mientras que `20020 m3` es volumen y no debe dividirse por 3 m para gobernar la superficie de construccion. | `02_Areas_construccion!C14` pasa a referenciar `07_MBR_activo!D11`, con factor `1.25` y area de implantacion `4550 m2` por reactor. Se crea `scripts/repair_qmedio_excel_mbr_area_errata.py` y no se alteran otros parametros del libro. |
| 2026-06-19 | Generar QGZ final corregido desde Excel/GPS-X8 sin seguir modificando otros oficiales | El usuario pidio corregir la errata del Excel y armar el QGZ final; para limitar el riesgo no se siguio tocando GPS-X, SVG oficial, CAD, visor ni memoria Word. | Se crea `apoyo/salidas_gemini/candidatos/candidato_20260619_qmedio_final_excel_corregido/` y el QGZ raiz `qgis_revision_EDAR_Qmedio_final_excel_corregido.qgz`. El GIS representa 2 decantadores y 2 MBR a escala de area corregida, mantiene todo dentro de `Zona`, rutas ortogonales, atributos Z/longitudes/codos/bombeo, y deja v4/v5 como antecedentes a revisar antes de cualquier promocion oficial posterior. |
| 2026-06-19 | Corrección de acople de biogás en planos y perfiles CAD | El auditor detectó un desacople de $2.83\text{ m}$ en la ruta `GAS-ANTORCHA` porque las coordenadas del centro de la planta de biogás estaban cableadas duras. | Se modificó `scripts/generate_profiles_plans.py` para leer dinámicamente el centroide de `G_SISTEMA_BIOGAS` desde `equipos_edar.gpkg`. Se ajustó la cota de salida a $+7.0\text{ m}$ sobre el terreno, y el auditor de planos ahora devuelve `CONFORME`. |
| 2026-06-19 | Integración de datos de tuberías y elevaciones de GIS en Excel | El usuario requirió inyectar los datos reales de longitud, codos, elevaciones y bombeos del GIS final corregido en el Excel oficial. | Se ejecutó `python scripts/integrate_hydraulics_to_excel.py` actualizando y recalculando los datos de las 35 tuberías y 17 bombeos en la hoja `04_Bombeo_tuberias` de `EDAR_Silvouta_Qmedio_linea_activa_ecuaciones_v9_revisado.xlsx`. |
| 2026-06-19 | Regeneración de Memoria Word Unificada | Mantener la total consistencia numérica de las cotas y alturas del análisis con el Word compilado. | Se re-ejecutó `python scripts/generate_word_informe.py` generando con éxito el reporte final unificado [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx). |
| 2026-06-19 | Separación de plantas/perfiles y simplificación topográfica | El usuario solicitó separar plantas y perfiles (el formato 1x2 anterior los deformaba y hacía muy pequeños) y simplificar la topografía de los perfiles. | Se modificaron los scripts para renderizar plantas y perfiles de agua y fango en páginas independientes A3 del PDF (resultando en 7 páginas totales). Se implementó interpolación lineal en `sample_route` para generar perfiles de terreno y tuberías continuos entre nodos, eliminando el ruido del DEM. El auditor fue actualizado y concluyó en CONFORME. |
| 2026-06-19 | Eliminar etiquetas de nodos intermedios y cotas de equipos en perfiles | El usuario pidió no nombrar "elevación por elevación" en los perfiles porque se genera una mancha de textos ilegible; solo deben quedar las etiquetas de identificación de la infraestructura (equipos). | Pendiente de aplicar: eliminar `ax.text(node.code)` en `draw_profile` (PDF, línea 1015) y `text(node.code)` en `draw_profiles_dxf` (DXF, línea 788). Simplificar la etiqueta del bloque de equipos en PDF (línea 1028) para mostrar solo `eq.code` sin la cota `({eq.coronacion:.1f} m)`. En DXF (línea 801) ya solo muestra `eq.code`. Después regenerar y auditar secuencialmente. |
| 2026-06-19 | Representar perfiles como cadena hidráulica con HGL y bombas | El usuario aclaro que el perfil debe servir para ver equipo tras equipo conectado por tuberias, con bombas asociadas y linea piezometrica inicial para saber donde hay que subir y cuanto. | `scripts/generate_profiles_plans.py` deja de etiquetar vertices intermedios en PDF/DXF, muestra solo equipos principales, incorpora HGL preliminar en naranja y anota saltos de bomba desde `conexiones_sugeridas.gpkg`. Se regeneran DXF/PDF/Excel/informe/manifest y `audit_profiles_plans.py` vuelve a dar `CONFORME`. |
| 2026-06-19 | Usar perfil hidraulico esquematico separado para aguas y fangos en el PDF | El usuario mostro la pagina 742 de `manual/proyecto_real/real.pdf` como referencia y pidio un perfil de aguas y otro de fangos, esquematicos y sin exceso de detalle. | Las paginas 3 y 5 de `4planos/EDAR_Perfiles_Plantas_v0.pdf` pasan a ser laminas hidraulicas de proceso: bloques de equipos, conducciones GIS con longitudes/codos, bombas preliminares y HGL. El Excel `EDAR_Perfiles_Plantas_v0.xlsx` agrega `40_Hidraulico_Aguas` y `41_Hidraulico_Fangos`. El DXF queda regenerado conforme pero mantiene su perfil longitudinal CAD. |
| 2026-06-20 | Corregir perfiles a "cuerda estirada" sobre traza real, manteniendo caracter esquematico | El usuario aclaro que el perfil debe desarrollarse cortando imaginariamente la linea real de aguas/fangos en planta: en planta puede ser serpiente, pero en perfil debe verse recta con PK acumulado, topografia real, equipos secuenciales y tuberias con distancia/pendiente. Tambien confirma que debe seguir siendo una representacion esquematica, con detalle suficiente pero sin exagerar. | `generate_profiles_plans.py` usa muestreo DEM sobre la ruta real, equipos colocados en su PK, tuberias con longitudes/pendientes y bombas/HGL cuando aportan informacion. Se omiten detalles constructivos secundarios y rotulos repetitivos. Decision implementada y validada el 2026-06-20. |
| 2026-06-20 | Usar geometria vertical esquematica relativa al terreno solo en las laminas de perfil | Las cotas antiguas de bloques producian una lectura donde casi toda la linea de agua parecia enterrada y desconectada de la topografia, aunque el objetivo es un esquema de anteproyecto. | El PDF/DXF coloca cada equipo en su PK real y conserva su altura relativa, pero relaciona graficamente la extension vertical con el DEM y conecta el bloque a la conduccion. Esta transformacion es solo visual: no sustituye cotas numericas GIS/Excel ni el calculo hidraulico final. |
| 2026-06-20 | Cerrar la primera version del perfil por cuerda estirada | Se implemento y reviso el criterio confirmado por el usuario para agua y fangos. | Se regeneran PDF, DXF, Excel, informe y manifiesto. Las tuberias principales muestran `L` e `i`; bombas y HGL se identifican sin duplicar textos. `audit_profiles_plans.py` devuelve `CONFORME` y las paginas 3 y 5 quedan revisadas visualmente. |
| 2026-06-21 | División horizontal del perfil desarrollado en dos subplots | Las infraestructuras se veían extremadamente delgadas debido a la gran diferencia entre el rango horizontal (330 m) y vertical (12 m) en un solo gráfico. | Se divide la página de perfiles de la línea de agua y fangos en dos secciones (0-200m y 200m-final), duplicando la escala física en el eje X para mejorar la visibilidad. Se añade una gestión de errores de archivo bloqueado (PermissionError) con salida alternativa. |
| 2026-06-21 | Unificación de perfil (sin partir), espaciado de etiquetas y leyendas | El usuario solicitó no partir el perfil si cabe entero, evitar el traslape de textos y contar con una leyenda clara para las líneas. | Se eliminó la partición en subplots, se implementó un algoritmo de relajación 1D para espaciar etiquetas horizontalmente (con líneas de directriz diagonales) y se inyectaron leyendas explícitas con nombres en español. |
| 2026-06-21 | Escala 1:1 en ejes X e Y y compresión extrema tipo acordeón | El usuario solicitó mantener la misma escala física en ambos ejes (X e Y) para evitar deformaciones verticales y comprimir las tuberías como acordeón. | Se cambió la relación de aspecto del gráfico a 'equal', se ajustó la compresión de tramos de tuberías al 5% (compresión agresiva tipo acordeón) y se regeneró el PDF de planos/perfiles. |
| 2026-06-21 | Corrección de signo de pendiente de gravedad y espaciado de etiquetas | Evitar el traslape de las etiquetas de tuberías comprimidas en acordeón y corregir el signo positivo de las pendientes por gravedad. | Se re-ubicó `space_out_1d` a nivel de módulo en `generate_profiles_plans.py`. Se implementó relajación horizontal 1D de etiquetas con líneas guía punteadas en PDF y DXF. Se forzó signo negativo (`slope = -abs(slope)`) en todas las conducciones de gravedad en planos, Excel y reportes. La auditoría volvió a dar `CONFORME`. |
| 2026-06-21 | Partición horizontal en 2 subplots, recálculo hidráulico y alternancia vertical de etiquetas | El usuario solicitó partir la hoja por la mitad horizontalmente para estirar el gráfico a escala 1:1, ampliar el tamaño de los equipos y evitar traslapes de textos de forma definitiva. | Se dividieron las páginas de perfiles en dos subplots apilados (superior: PK 0 a la mitad, inferior: mitad a final), duplicando la escala visual del dibujo. Se recalcularon las cotas Z de conexiones y nodos de ruta en base a `COTA_REF` (consistente con las soleras reales), logrando que los tramos de gravedad desciendan físicamente (pendiente negativa real) y solo suban en impulsiones. Se alternaron las etiquetas arriba/abajo del tubo relajándolas de forma separada. La auditoría dio CONFORME. |
| 2026-06-21 | Estiramiento horizontal (compresión 0.45) y optimización de márgenes en subplots del PDF | El usuario solicitó estirar la escala en ambos lados del perfil dividido para llenar la hoja y evitar textos pequeños o traslapados. | Se incrementó el factor de compresión de acordeón a 0.45 y se redimensionaron las cajas de subplots en el PDF a 90% de ancho y 36% de alto ([0.05, 0.48, 0.90, 0.36] y [0.05, 0.09, 0.90, 0.36]), maximizando el espacio de dibujo y ampliando la escala física resultante sin comprometer el aspecto 1:1 ('equal'). La auditoría dio CONFORME. |
| 2026-06-21 | Sustituir definitivamente el acordeon por PK horizontal lineal en los perfiles PDF | La revision visual detecto que una transformacion no lineal del PK podia inducir una lectura falsa de distancias y que las flechas rectas duplicaban la conduccion continua. | Agua y fangos se mantienen en dos paneles, pero cada panel usa PK real lineal. La vertical queda declarada como esquematica; cada tramo se representa una sola vez y el sentido se marca sobre esa conduccion. Las plantas PDF omiten puntos en vertices y la auditoria final vuelve a dar `CONFORME`. Esta decision sustituye las decisiones de compresion tipo acordeon como regla vigente. |
| 2026-06-21 | Usar las plantas de agua y fangos como mapas de orientacion de sus perfiles | El perfil desarrollado necesita una referencia espacial clara para entender donde comienza, por donde discurre y donde termina dentro de la implantacion. | Las hojas 2 y 4 resaltan la ruta principal sobre la planta, con halo, flechas de sentido, inicio, fin y PK total. El paquete conserva 7 hojas: mapa general; planta+perfil de agua; planta+perfil de fangos; biogas y olores como agregados. |
| 2026-06-21 | Generar dos Excel: standalone hidraulico (Phase 1) y v10 con Qpunta (Phase 2) desde GIS | El usuario solicito completar la etapa pendiente de Excel final hidraulica usando `conexiones_sugeridas.gpkg` como fuente unica y el v9 como plantilla para Phase 2. | `scripts/_generate_phase1.py` lee 35 conexiones GIS, calcula Swamee-Jain/Darcy-Weisbach, y genera: (1) `EDAR_Hidraulica_Bombeo_Tuberias_v1.xlsx` con 5 hojas standalone; (2) `EDAR_Silvouta_Qmedio_linea_activa_v10_bombas.xlsx` con 26 hojas (23 originales + 3 nuevas), `04_Bombeo_tuberias` ampliada con 7 columnas Qpunta activas. |
| 2026-06-21 | Usar Swamee-Jain para factor de friccion y Darcy-Weisbach para perdidas de carga | El v9 usaba una aproximacion simplificada `0.02*(L/D)*(V²/2g)` para hf. Para el calculo independiente de Phase 1 se requeria un metodo mas riguroso y trazable. | Se implemento Swamee-Jain: `f = 0.25/[log10(ε/(3.7D) + 5.74/Re^0.9)]²` con ε = 1.5e-6 m (PVC) y ν = 1.0e-6 m²/s. hf = f·(L/D)·(V²/2g). hm = num_codos × 0.5 × V²/2g. |
| 2026-06-21 | Ampliar v9 a v10 con columnas Qpunta en lugar de hoja separada | El usuario pidio no crear una hoja nueva de Qpunta sino ampliar `04_Bombeo_tuberias` con 7 columnas adicionales (W:AC) con formulas activas Excel. | Las columnas Qpunta referencian las celdas existentes: Qpunta diseño = `IF(B="olores", E, E*1.3)`, velocidad/hf/hm/HMT/potencia Qpunta recalculan con el Qpunta correspondiente. Las formulas diferencian olores (factor 1.2, /3600) de agua (factor 1.3, /86400). |
| 2026-06-21 | Diametros internos por tipo de linea con overrides puntuales | El GIS no contiene diametros; se necesitaba una asignacion consistente con criterios de ingenieria para el calculo hidraulico. | agua=0.9m, agua secundaria=0.6m (overrides: A05→A06a/A06b, A08a/A08b→A10), fangos=0.1m, quimicos=0.05m, retornos/biogas=0.15m, olores=0.5m. Valores provisionales hasta definicion de ingenieria de detalle. |
| 2026-06-21 | Correccion de consistencia fisica de caudales especificos en Python | Las bombas de fangos, biogas y quimicos presentaban potencias de megawatts/gigawatts porque el script usaba el caudal total de agua de la planta por error en Python. | Se modificaron los caudales en Python para usar los valores de diseno de cada tramo (sludge 500/750 m³/d, biogas 2407 m³/d, etc.), normalizando las potencias a rangos reales y fisicos. |
| 2026-06-21 | Correccion de diametro de conducto de FeCl3 y recalculo COM en Excel | Los conductos FeCl3 a MBR (T-10 y T-11) tenian 50mm de diametro en v9 pero llevaban el caudal principal de agua, provocando perdidas de 57,000m y velocidad de 379 m/s en Excel. | Se cambio el diametro de T-10/T-11 a 0.6m en Python y se forzo su escritura en la columna N de Excel. Se recalculo el libro v10 mediante COM de Excel en Windows y se audito confirmando cero errores de celdas. |
| 2026-06-21 | Integrar bombas GIS en el gemelo 3D | El geometry.json tenia 2,474 tuberias reales del GPKG pero 0 bombas, pese a que 17 de las 35 conexiones tienen `requiere_bomba='si'` y `altura_bombeo_m` en el GIS. | `generate_digital_twin.py` coloca `add_pump()` en el vertice inicial de cada tuberia con bombeo, con radio/altura proporcionales y color por tipo de linea. Se regenero `geometry.json` con 17 bombas. |
| 2026-06-21 | Eliminar modelo ASM1 del gemelo digital; migrar a datos GPS-X 8.0 | El gemelo digital usaba constantes cineticas ASM1 (`mu_h`, `K_S`, `Y_H`, etc.) de librerias Python propias que no estaban validadas contra GPS-X 8.0. | `simulate_wwtp_dynamics.py` reescrito completamente con perfil horario sinusoidal escalado al punto de diseno GPS-X 8.0 OUTPUTS_PLUS max. `simulation_results.json` regenerado con 24 horas GPS-X 8.0. |
| 2026-06-21 | Desacoplar modelo parametrico Python (`generate_edar_simplified`) del gemelo digital | `generate_digital_twin.py` importaba `base_inputs`, `compute` y `equipment_rows` del modelo parametrico, creando dependencia circular y valores no validados contra GPS-X. | `generate_digital_twin.py` ahora lee directamente del Excel GPS-X 8.0 via `openpyxl` + helper `_read_gpsx8_value()`. `add_simplified_edar()` ya no recibe el parametro `c`. |
| 2026-06-21 | Mejora estetica del visor 3D con renderizado fisicamente correcto | La estetica anterior era basica y poco profesional para defensa de proyecto. | `app.js` usa ACESFilmicToneMapping, HemisphereLight + DirectionalLight, fog, sombras PCFSoft, 5 graficos Chart.js incluyendo energia/biogas. `index.html` usa glassmorphism CSS y variables de color. |
| 2026-06-21 | Representar la calle como camino plano ancho, no como tuberia cilindrica | El usuario indico: "recuerda hacer el calle un camino plano, no una linea". La calle se representaba como polilinea cilindrica tipo tuberia que no reflejaba la realidad fisica de un vial. | Nuevo tipo geometrico `road` en Python (`add_road_segment`, `add_gis_road`) y Three.js (BoxGeometry de 6m ancho × 0.18m espesor, color asfalto, rotado y anclado al DEM). 6 segmentos planos reemplazan los antiguos segmentos pipe-style. |
| 2026-06-21 | Optimizar rendimiento del gemelo 3D eliminando redundancia geometrica | geometry.json pesaba 5.2 MB con 11,907 objetos (76% esferas de junta redundantes). El usuario pregunto si usar KMZ/QGIS y pidio simplificar, eliminar codos y otras mejoras. | RDP (Ramer-Douglas-Peucker, epsilon=1.5m) en polilineas GIS, deduplicacion de juntas (solo en extremos), pipes diagonales directos sin descomposicion ortogonal, celdas de terreno reducidas (step 8→22). geometry.json pasa a 169.8 KB con 379 objetos (-96.7% tamano, -96.8% objetos). InstancedMesh descartado por innecesario a esta escala y porque romperia raycasting/TransformControls. Auditoria actualizada para aceptar ausencia de juntas como optimizacion. |
| 2026-06-21 | Eliminar edicion 3D y reordenar graficos del visor | El modelo GIS es fijo y la edicion (TransformControls, arrastre, guardado/descarga) ya no se necesita. Los graficos estaban en orden que no reflejaba el flujo logico de proceso. | Eliminados de web/: edit-panel HTML, TransformControls.js CDN, instanciacion/listeners de TransformControls, raycaster de seleccion, selectObject/deselectObject/updateUIInputs/onUIInputChange/onSelectedObjectMove/saveLayoutToServer/downloadLayoutAsJSON, y event listeners asociados. Tooltip hover GPS-X conservado (lectura, no edicion). Graficos reordenados: caudal → organico → nitrogeno → proceso → energia. Calle ya era road; alturas GIS ya se leian de equipos_edar.gpkg. No se regenero geometry.json (solo cambios UI). |
| 2026-06-21 | Triangulacion del terreno desde DEM en vez de celdas sueltas | Las celdas de terreno individuales (terrain_cell) generaban muchas geometrias sueltas y no representaban fielmente la topografia real del DEM. El usuario pidio triangular directamente desde el raster. | Nuevo tipo `terrain_mesh` en Python y JS: un solo `BufferGeometry` indexado con ~1.026 vertices coloreados por elevacion (verde→naranja) y ~1.698 indices de triangulos. Muestreo step=6 sobre el DEM con mascara de nodata. Renderizado en Three.js con `vertexColors: true`, `DoubleSide`, opacidad 0.62 y espejado X via `toSceneX()`. Eliminada la funcion `add_terrain_cell()`. |
| 2026-06-21 | Remover graficos Chart.js del visor 3D temporalmente | El usuario pidio eliminar los graficos por ahora para centrarse en la geometria 3D y las etiquetas. | Eliminados de web/: CDN de Chart.js, panel `#charts-panel` con 5 canvas, controles de simulacion (slider, play/pause). Eliminado de app.js: `Chart.register`, `verticalLinePlugin`, `webCharts`, `initCharts()`, `updateChartsHour()`, `loadSimulationData()`, `initSimulationUI()`, variables `simData`/`currentHour`/`isPlaying`/`playInterval`. `fetch` simplificado a solo `buildPlant()`. `getDynamicTooltipContent()` simplificado. |
| 2026-06-21 | Gestion de etiquetas con toggle de leyenda independiente | Las etiquetas tapaban demasiado la visualizacion 3D. El usuario pidio poder ocultarlas con un toggle de leyenda. | Nueva linea `L_ETIQUETAS = "Etiquetas"` en Python. Las 33 etiquetas reasignadas a esta linea. HTML: boton "Etiquetas/Nombres" en la leyenda con color-box blanco. JS: `LINE_LABELS` y `PROCESS_LINES` incluyen `'Etiquetas'`; el sistema de toggle `activeLines`/`updateLineVisibility()` permite ocultar/mostrar todas las etiquetas independientemente. |
| 2026-06-22 | Crear V10 full ecuaciones sin sustituir GPS-X oficial | El profesor observo pocos calculos y ausencia de referencia GPS; el usuario pidio potenciar Excel comparando con GPS-X como fuente oficial. | Se crea `EDAR_Silvouta_Qmedio_linea_activa_v10_full_ecuaciones_GPSX.xlsx`: Excel explica, predimensiona y reconcilia mediante formulas activas, mientras GPS-X 8.0 `OUTPUTS_PLUS/max` conserva la autoridad de simulacion. La comparacion de efluente usa objetivo Excel frente a resultado GPS-X, sin fingir una segunda simulacion biologica. |
| 2026-06-22 | Separar propiedades de agua, fango, biogas y aire en la hidraulica V10 | `v10_bombas` aplicaba la ecuacion de agua a la desodorizacion y producia potencias del orden de 18 MW. | La V10 usa densidad, viscosidad y rugosidad por fluido, Reynolds, Swamee-Jain, Darcy-Weisbach y `rho*g*Q*H/eta`; para gases no incorpora desnivel geodesico como carga estatica. La ventilacion calculada baja a 29,35 kW y queda como anteproyecto pendiente de fabricante. |
| 2026-06-22 | No ocultar la deriva de hash del XLS GPS-X max | El archivo `max/plantaedar.xls` actual no coincide con el SHA-256 registrado en la fuente funcional JSON. | Los valores del JSON de control siguen gobernando la V10, se registra el hash actual `a578c03c...`, y `26_Control_ecuaciones!C10` queda en `REVISAR` hasta reconciliar o reexportar la fuente. |
| 2026-06-22 | Unificar V11 sin renombrar las hojas de proceso existentes | El usuario pidio reunir full, bombas y supermodo ecuaciones, pero las hojas `01-22` ya son contratos usados por formulas y entregables. | La V11 conserva las 23 hojas de proceso y renumera solo los modulos agregados: bombas `23-24`, soporte/GPS-X/control `25-29` y supermodo `30-32`. El resumen unificado se coloca primero sin modificar las V10. |
| 2026-06-22 | Supermodo ecuaciones con formula visible y catalogo enlazado | Una defensa tecnica necesita demostrar que el resultado procede de una ecuacion activa, no de un valor pegado. | Las hojas de proceso, hidraulica, bombas y comparacion muestran `FORMULATEXT` junto al resultado. `30_Catalogo_Formulas` inventaria 3.054 formulas operativas con resultado vivo; mapa y auditoria verifican cobertura y errores sin crear referencias circulares. |
| 2026-06-22 | Excluir mapa y auditoria del catalogo operativo para evitar circularidad | Catalogar formulas que cuentan o auditan el propio catalogo generaria dependencias circulares. | El catalogo cubre el 100% de las formulas operativas previas a las capas auto-referenciales; `31_Mapa_Unificado` y `32_Auditoria_Supermodo` se controlan separadamente. La validacion final confirma 15.402 formulas totales y 0 referencias circulares. |
| 2026-06-22 | Entregar una variante V11 con el formato visual original | El usuario pidio retirar el formato azul y volver al aspecto del libro original. | Se conserva la V11 azul como antecedente y se crea `..._Formato_Original.xlsx` con fondo blanco, texto negro Calibri, bordes negros y graficos grises. El cambio es exclusivamente visual: 34 hojas, 15.402 formulas y resultados permanecen iguales. |
| 2026-06-22 | Reparar referencias vacías en la columna E (GPS-X8) de las hojas de proceso y resumen | Las celdas de columna E vacías hacían que las fórmulas de comparación en la columna F evaluaran a cadena vacía `""` ("apuntar a la nada"). | Se implementaron fórmulas activas en la columna E vinculando las variables calculadas con los datos del modelo correspondientes (`E9*E10`, `E17*E18/100`, `ROUNDUP(E13/20,0)`, `IF(E6<=25,"Cumple","No cumple")`, etc.), eliminando 52 celdas vacías indirectas en la auditoría de celdas seguras y asegurando la coherencia física en el libro de formato original. |
| 2026-06-23 | Merge quirúrgico de `web/app.js` en lugar de sobrescritura total | El manual solo proporcionaba la lógica de UI/datos (~435 líneas: `UNIDADES_DATA`, paneles, raycasting click, exageración vertical, resize) pero no incluía la infraestructura de escena Three.js existente (601 líneas: cámara, renderer, OrbitControls, iluminación, `buildPlant()`, animación de agua). Una sobrescritura habría roto el visor 3D. | Se implementó un merge de 15 bloques (A-O): se preservó la infraestructura de escena (líneas 1-378), se reemplazó el hover `mousemove` por click `raycaster`, se integró `UNIDADES_DATA` (29 equipos), se actualizó `showProfileMode()` para checkboxes `.layer-chk`, se corrigió `resize()` con `getBoundingClientRect()`, y se eliminó `INTERSECTED` en favor de `SELECTED_MESH`. Resultado: 885 líneas unificadas. |
| 2026-06-23 | Implementación delegada a modo Code con especificación precisa de 15 bloques | El modo project-manager solo puede editar archivos `.md`. Los 4 archivos a modificar son `.html`, `.js`, y `.css`. | Se delegó la implementación a modo Code mediante `new_task` con la especificación completa de los 15 bloques de merge, el contenido exacto de cada archivo del manual, y los pasos de verificación. Code mode ejecutó las 5 operaciones (crear dir, 2 creates, 1 overwrite, 1 merge) y verificó `resize()`, selectores, y arranque del servidor. |
# Registro de Decisiones (DECISIONS.md)

## DSTA - EDAR Silvouta

| Fecha | Decisión | Motivo | Consecuencias |
|-------|----------|--------|---------------|
| 2026-05-20 | Migración a formato texto de manuales | Permitir que la IA pueda leer los documentos PDF (que estaban fallando por OCR o NotebookLM expirado) | Se han creado archivos .txt en `DSTA/manual/txt`. |
| 2026-05-20 | Cambio de geometría abstracta a ortogonal | Las tuberías "voladoras" no representaban la realidad física de la planta. | El modelo 3D ahora usa colectores ortogonales de 90 grados y ramales, simulando construcción civil real. |
| 2026-05-20 | Modificación del Layout del Excel a 4 Líneas y Decantación Rectangular | Para cuadrar matemáticamente con las exigencias del pliego técnico y la imagen de planta real (`layout_12_3.jpeg`). | Se actualizaron las fórmulas de `generate_edar_excel.py` para prismas rectangulares. |
| 2026-05-21 | Ordenamiento de la Línea de Fangos (Flujo Hidráulico Sur) | Las tuberías de lodos iban "hacia atrás" en el eje X. | Se reestructuraron las coordenadas Z y X de la zona sur (Espesadores, DAF, Digestores, Deshidratación) para que fluyan de izquierda a derecha en paralelo a la línea de agua. |
| 2026-05-21 | Inclusión de Gradiente de Colores de Depuración | Mejorar la interpretación visual de las fases de depuración y digestión de fangos en el visor 3D. | El JSON de geometría incluye ahora colores hexadecimales dinámicos según el avance de la depuración. |
| 2026-05-21 | Inclusión de Tornillos de Arquímedes | Simular la obra de llegada desde colectores profundos con el máximo rigor. | Se programó una nueva función `add_diagonal_pipe` apoyada por un algoritmo de rotación Quaternion en Three.js. |
| 2026-05-21 | Juntas visibles y colectores explícitos en tuberías 3D | La vista 3D mostraba cortes aparentes en tuberías aunque algunas conexiones existían como coordenadas implícitas. | `scripts/generate_digital_twin.py` añade nodos esféricos en extremos/codos, colector de salida de biológicos y colector sur WAS antes de regenerar `web/data/geometry.json`. |
| 2026-05-21 | Contenedor de gruesos junto al pozo | La obra de llegada necesitaba un punto visible para que la cuchara bivalva descargue los residuos gruesos. | Se incorpora la línea visual `rechazos` con un contenedor abierto y sólidos retenidos al lado del pozo de gruesos. |
| 2026-05-21 | Uso de `real.pdf` como contrato de auditoría de líneas | El usuario aportó el PDF real para asegurar que las líneas de agua, fangos, biogás y rechazos no se validen solo por inspección visual. | Se extrajo `manual/txt/proyecto_real_real.txt`, se renderizaron las páginas 133 y 134, y se creó `LINEAS_PROCESO_REAL.md`. |
| 2026-05-21 | Auditor reproducible de líneas del Gemelo Digital | La corrección visual de tuberías no garantiza conformidad de proceso. | Se añadió `scripts/audit_digital_twin_lines.py`; el JSON queda geométricamente sano, pero el proceso actual no es conforme al PDF real por ausencia de MBR, hidrólisis térmica, escurridos, cogeneración y otros equipos. |
| 2026-05-21 | Eliminación de tramos superpuestos exactos | Algunos ramales repetían el mismo tramo de colector y podían generar sobredibujado visual. | `generate_digital_twin.py` usa colectores comunes únicos antes de ramificar hacia primarios, biológicos, secundarios, espesadores y gasómetro. |
| 2026-05-21 | Normalización de líneas de leyenda en el JSON 3D | El tooltip mostraba códigos genéricos (`aguas`, `fangos`) que no permitían verificar las cinco líneas del visor. | `generate_digital_twin.py` clasifica cada elemento en líneas normalizadas; `web/app.js` muestra etiquetas legibles y el auditor verifica recuentos por línea. |
| 2026-05-22 | Leyenda interactiva y servidor con URL directa | La revisión visual por capas requería apagar/encender líneas y evitar adivinar el puerto disponible. | `web/index.html` y `web/app.js` convierten la leyenda en filtros por línea; `scripts/serve_web.py` busca puerto local libre e imprime `http://localhost:PUERTO/web/`. |
| 2026-05-22 | Migración del Gemelo Digital al contrato real MBR + fangos avanzados | El auditor contra `real.pdf` exigía sustituir el esquema simplificado por el proceso real de Silvouta. | `generate_digital_twin.py` modela MBR, reactores biológicos abiertos con agua transparente y aireadores, línea completa de fangos con hidrólisis térmica, escurridos, biogás, cogeneración y antorcha; la auditoría devuelve `CONFORME`. |
| 2026-05-22 | Conexión rigurosa de bombas y cota de aireadores | Análisis bibliográfico identificó aireadores sumergibles (que deben apoyar en el fondo, no flotar) y visualmente se auditaron bombas sin tubos de conexión. | Se conectaron explícitamente las succiones/impulsiones de bombeo y se bajó la cota Y de los aireadores al nivel de la solera del tanque biológico. |
| 2026-05-27 | Uso de fórmula de Darcy-Weisbach / Swamee-Jain en Excel | El modelo necesitaba cálculos de pérdidas de carga precisos para la línea piezometrica. | Se reemplazó la pestaña de bombeos por un cálculo detallado para 6 estaciones de bombeo basadas en `real.pdf`. |
| 2026-05-27 | Generación de Plano Base en formato CAD (.dxf) | Necesidad de pasar del gemelo digital conceptual a un plano editable topográficamente (Planta y Perfil) para Civil 3D. | Se creó el script `generate_dxf_plan.py` usando `ezdxf` para dibujar en coordenadas locales y mallas. |
| 2026-05-27 | Inclusión forzada de Perfil Hidráulico de Fangos | Requisito explícito del profesor/usuario, a pesar de que el fango discurre presurizado y no suele dibujarse en perfil. | Se añadió un bloque de dibujo en el DXF con el perfil secuencial de equipos de fangos y sus bombas. |
| 2026-05-27 | Auditor reproducible del Excel paramétrico | La revisión manual detectó que `check_excel.py` confundía cachés de fórmula no recalculadas con resultados cero reales. | Se añadió `scripts/audit_excel_model.py`, que audita fórmulas y referencias internas sin depender de valores cacheados. |
| 2026-05-27 | Corrección de referencias de fórmulas del Excel | Varias fórmulas apuntaban a filas de encabezado, celdas vacías o a sí mismas en bases, fangos y bombeos. | Se corrigió `scripts/generate_edar_excel.py`, se regeneró `EDAR_Modelo_Parametrico.xlsx` y la auditoría ya no detecta referencias vacías. |
| 2026-05-27 | Plano 04 V4 como CAD paramétrico de ingeniería | El DXF anterior se percibía como conceptual y no como diseño de ingeniería. | `generate_dxf_plan.py` ahora genera `plano_04_v4_ingenieria.dxf` con capas por disciplina, cotas, equipos etiquetados, tablas de parámetros, cuadros de tuberías/bombeos, perfiles y detalle MBR; `audit_dxf_plan.py` valida densidad mínima. |
| 2026-05-27 | Formulario de cálculo dentro del informe Word | El Anejo 2 debía mostrar explícitamente las fórmulas usadas en cada caso, no solo remitir al Excel. | `ESQUELETO_INFORME.md` y `ESQUELETO_INFORME.docx` incluyen 7 tablas de fórmulas; `scripts/generate_word_informe.py` queda como generador reproducible del Word. |
| 2026-05-27 | Diagrama de flujo en SVG editable | El usuario necesita un diagrama tipo proceso, como el ejemplo aportado, pero para el proyecto y editable en Inkscape. | Se creó `diagrama_flujo_edar_silvouta.svg` con capas de agua, fangos, biogás y retornos; `scripts/generate_process_flow_svg.py` calcula y rotula los datos principales del modelo. |
| 2026-05-27 | Excel separado por entradas, resumen y cálculos por línea | La revisión del usuario pidió una hoja de datos introducidos, hojas independientes de cálculo y un resumen de resultados siguiendo línea de aguas, fangos y otras líneas importantes. | `generate_edar_excel.py` regenera `EDAR_Modelo_Parametrico.xlsx` con 10 hojas ordenadas: datos, resumen, agua, fangos/biogás/deshidratación y bombeos. Las fórmulas quedan trazables y con cachés numéricas calculadas. |
| 2026-05-27 | Incorporación de criterios de Temas 1-6 al Excel | El usuario pidió revisar `Tema 1.pdf` a `Tema 5 y 6.pdf` para detectar datos faltantes del Excel. | Se añadió `02_Datos_Faltantes` y se incorporaron caudales, subproductos de pretratamiento, lamelar, bombeo primario y aireación; `REVISION_TEMAS_EXCEL.md` documenta qué se incorporó y qué sigue pendiente. |
| 2026-05-27 | Georreferenciacion e Inversion de Flujo EDAR | El usuario indico que el flujo real es de Oriente a Poniente y aporto los DEM MDT02. | Se creo plano_05_georeferenciado_topografia.dxf extrayendo curvas de nivel del TIF e invirtiendo el eje X del layout. |
| 2026-06-03 | Alcance academico simplificado para informe y Excel | La consigna y los detalles de evaluacion muestran que el profesor pide predimensionamiento defendible, no replicar el proyecto real completo. | Se creo `PLAN_ANALISIS_SIMPLIFICADO.md`; la solucion base queda centrada en linea de agua con MBR, linea de fangos con digestion anaerobia y deshidratacion, y linea de gas con biogas, energia y antorcha. La hidrolisis termica y otros bloques avanzados del proyecto real se trataran como referencia/mejora, no como nucleo de calculo. |
| 2026-06-03 | Extraccion trazable de fuentes de `manual/proyecto_real` | `TRABAJO DESGLOSADO.pdf` y `DETALLES EVALUACION Y EXAMEN.docx` son fuentes directas de evaluacion y debian estar disponibles para busqueda y trabajo con IA. | Se generaron TXT derivados en `manual/txt/` para los PDF principales de `manual/proyecto_real` y para el DOCX de evaluacion mediante extraccion zip/xml. |
| 2026-06-03 | Incorporacion de linea de olores al alcance simplificado | El usuario confirmo que debe agregarse linea de olores y la consigna la considera contenido valorable. | `PLAN_ANALISIS_SIMPLIFICADO.md` pasa a tratar olores como cuarta linea funcional, con focos, captacion, caudal de aire, desodorizacion y descarga. El Excel simplificado debera incluir hoja `35_Linea_Olores`. |
| 2026-06-07 | Implantacion condicionada por capas GIS de `1gis/Carga` | El usuario confirmo que `Zona` es limite fijo, que `Aliviadero Actual` es la entrada por gravedad, que el vertido debe ir al extremo izquierdo del meandro occidental del rio y que la gestion/transporte de fangos debe quedar cerca de la calle. | La propuesta de implantacion no debe ampliar la parcela; debe dibujar una conexion exterior desde el aliviadero, organizar la linea de agua de este a oeste, reservar descarga al meandro occidental y situar deshidratacion/silo de fangos junto al acceso viario. |
| 2026-06-07 | Base futura oficial para dimensionar la EDAR completa | El usuario pidió decidir la EDAR a partir del caudal en m3/d, afluente y efluente. El Anejo 1 contiene una base contractual completa para situación futura: Qmedio 64.800 m3/d, concentraciones de entrada y objetivos de vertido. | El Excel simplificado y la memoria se reconstruirán sobre la situación futura del Anejo 1. `EDAR_Modelo_Parametrico_v1.xlsx` se usará como referencia de valores oficiales, pero no como base cerrada sin auditar; `v0` queda como referencia académica previa. |
| 2026-06-07 | Modelo simplificado con huellas de equipos como base de implantacion | El usuario pidio generar la EDAR completa recordando los tamanos de los equipos y el espacio a escoger. | Se creo `scripts/generate_edar_simplified.py` y `2planilla/EDAR_Modelo_Simplificado_v0.xlsx`; la implantacion futura debera usar la hoja `60_Equipos_Espacios` y no ubicar equipos sin comprobar su huella, margen de servicio, viales y reserva. |
| 2026-06-07 | Gemelo Virtual migrado al contrato simplificado + GIS | El gemelo 3D anterior seguia el contrato avanzado del PDF real; el proyecto actual requiere representar la EDAR simplificada dimensionada con el Excel nuevo y las condiciones GIS indicadas por el usuario. | `scripts/generate_digital_twin.py` ahora genera `web/data/geometry.json` con aliviadero exterior, parcela, calle, rio/meandro occidental, agua, fangos, gas/energia y olores. `scripts/audit_digital_twin_lines.py` audita contra `EDAR_Modelo_Simplificado_v0.xlsx` y criterios GIS 2026-06-07; `web/index.html` y `web/app.js` incorporan la linea de olores a la leyenda. |
| 2026-06-07 | Diagrama de flujo base alineado con el modelo simplificado | El diagrama SVG anterior usaba datos del modelo parametrico previo y podia confundir la defensa del proyecto actual. | `scripts/generate_process_flow_svg.py` genera `0diagrama/diagrama_flujo_edar_silvouta_simplificado_v1.svg` con Qmedio 64.800 m3/d, BNR, MBR, FeCl3, fangos, biogas/energia, olores, retornos y notas de implantacion. |
| 2026-06-07 | Gemelo Virtual GIS como base visual, no rectangulo conceptual | El usuario detecto que el 3D no mostraba topografia, rio, poste electrico, calle ni punto de carga, y que habia equipos visualmente solapados. | `generate_digital_twin.py` pasa a leer `1gis/Carga` directamente: DEM, curvas, `Zona`, `Calle`, `rio` e `importantes`. Se añaden etiquetas visibles, terreno, poste electrico, punto de carga de fangos, camion, vertido occidental y auditoria de solapes AABB en huellas principales. |
| 2026-06-07 | Paquete reproducible de plantas y perfiles por linea funcional | El usuario pidio perfiles y plantas de linea de agua, fangos, gases y olores en DXF/PDF, con calculos Excel, informe, topografia, rio, calle, poste electrico, punto de vertido y punto de carga de fangos. | Se crea `scripts/generate_profiles_plans.py`, que genera `4planos/EDAR_Perfiles_Plantas_v0.dxf`, `4planos/EDAR_Perfiles_Plantas_v0.pdf`, `2planilla/EDAR_Perfiles_Plantas_v0.xlsx`, informe Markdown/PDF y manifiesto. Las plantas quedan en UTM EPSG:25829 y los perfiles se calculan sobre `TopoDEM.tif` como rasantes preliminares de anteproyecto. |
| 2026-06-08 | Acople DXF-gemelo como condicion de validacion | El usuario recalco que los DXF deben estar adecuados al gemelo digital. La primera version tenia algunas huellas agregadas en planta aunque el gemelo mostraba unidades individualizadas. | `generate_profiles_plans.py` dibuja BNR, MBR y depositos tampon como unidades separadas alineadas al gemelo. Se añade `scripts/audit_profiles_plans.py`, que valida capas, rutas, perfiles, Excel/PDF/manifiesto y coincidencia de nodos/equipos del DXF con anclajes de `web/data/geometry.json`. |
| 2026-06-08 | Transformacion visual independiente para el visor web | El usuario detecto que el Gemelo Digital se leia invertido frente a las imagenes GIS de `1gis/images`, aunque `geometry.json` y los DXF conservaban coordenadas GIS coherentes. | `web/app.js` mantiene los datos fuente en X=Este y Z=Norte, pero renderiza con `X_visual = -X_GIS` para que la vista Planta de Three.js coincida con QGIS. Los DXF y manifiestos no se invierten y siguen siendo la referencia georreferenciada. |
| 2026-06-09 | Cache-bust y Planta superior fija para el visor | La inversion seguia apareciendo al usuario, probablemente por una combinacion de script cacheado y transicion oblicua de camara en la vista `Planta`. | `web/index.html` carga `app.js?v=20260609-orientacion-final`, `web/app.js` versiona `geometry.json`, reemplaza la animacion por presets instantaneos y bloquea rotacion en `Planta`; `scripts/serve_web.py` sirve con no-cache. |
| 2026-06-09 | Integración de bases de diseño, justificación MBR y arquitectura del Gemelo en la Memoria | El usuario solicitó registrar las bases de diseño de calidad de agua, la justificación de MBR vs. convencional en la parcela útil GIS, y las librerías necesarias para el gemelo digital real. | Se modificó `3informe/ESQUELETO_INFORME_v0.md` insertando las cargas y concentraciones calculadas, detallando la viabilidad del MBR por restricciones espaciales de la parcela (3.73 ha) y proponiendo la arquitectura de librerías open-source (wwtpsim, ezdxf, epanet, etc.). |
| 2026-06-09 | Trazados ortogonales, codos hidráulicos y gráficos de simulación | El usuario solicitó eliminar intersecciones de tuberías con reactores, añadir pérdidas menores por codos de 90° con elevaciones por bombeo y mostrar gráficos temporales interactivos de simulación. | Se modificaron los trazados de tuberías en el DXF y el gemelo 3D para que discurran por pasillos, se incorporó Darcy-Weisbach y Swamee-Jain con pérdidas por codos y potencia de bombeo en el Excel y la simulación, y se integró un panel de 4 gráficos en tiempo real con Chart.js en la web. Ambas auditorías devuelven CONFORME. |
| 2026-06-09 | Solución al estiramiento/redimensionamiento infinito de gráficos | Los canvas de Chart.js sufrían un bucle de redimensionamiento (resize loop) al renderizarse en una caja flexbox con barras de scroll. | Se encapsularon los lienzos canvas en contenedores relativos `.chart-wrapper` con altura fija de 110px y `overflow: hidden`, configurando los canvas como elementos absolutos para cortar la realimentación del bucle. |
| 2026-06-10 | Sincronización del diseño hidráulico y trazado físico de tuberías | El usuario indicó que las cotas, codos y bombas de la EDAR deben responder estrictamente a los cálculos hidráulicos realizados. | Se actualizaron las longitudes y codos reales del trazado 3D en el script del Excel (B1: 70m, 3 codos; B5: 205m, 5 codos) recalculando pérdidas manométricas y potencias de motores. Se resolvieron los tramos de tuberías duplicados y se verificó conformidad en ambas auditorías. |
| 2026-06-10 | Redacción y unificación de Anejos 1-6 | Cumplir estrictamente con el esqueleto académico exigido y formalizar las secciones ausentes (Anejo 3, 5 y 6). | Creación de [MEMORIA_Y_ANEJOS_v1.md](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.md) conteniendo la justificación MBR, el predimensionamiento biológico/MBR y las secciones de energía/olores. |
| 2026-06-10 | Vinculación del Word a cálculos paramétricos de Python | Asegurar la consistencia numérica absoluta entre el libro Excel y la memoria técnica escrita. | Modificación de [generate_word_informe.py](file:///d:/projects/DSTA/scripts/generate_word_informe.py) para inyectar dinámicamente las variables de simulación en las tablas y textos del informe. Generación de [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx). |
| 2026-06-10 | Editor de Layout 3D interactivo y persistencia | Permitir al usuario reubicar y ordenar los equipos y tuberías en la planta desde la web y persistir la configuración en geometry.json, así como aislar los perfiles de proceso en vistas ortogonales. | Integración de TransformControls de Three.js y panel flotante de coordenadas XYZ en index.html/app.js. El movimiento de unidades traslada de forma solidaria sus sub-elementos (mallas de agua, difusores, etiquetas) y calcula extremos locales para tuberías. Se añadió el método HTTP POST /save-layout en serve_web.py para persistencia directa. |
| 2026-06-11 | Uso exclusivo de GPS-X 8.0 full para el paquete de simulacion | El usuario confirmo que la licencia full corresponde a GPS-X 8.0 y que GPS-X 9.0 es limitada y debe ignorarse. | `scripts/generate_gpsx_package.py` apunta a `C:/GPS-X80`, el paquete queda en `gps_x/DSTA_Silvouta_MBR_GPSX8/` y la version 9.0 no se usa como fuente ni destino. |
| 2026-06-11 | Layout compatible `06mbr1` dentro del paquete GPS-X | GPS-X 8.0 puede conservar referencias internas al basename original de la plantilla en sidecars o binarios. | La ruta recomendada de apertura es `layout_compat_06mbr1/06mbr1.lyt`; la copia renombrada DSTA se mantiene para trazabilidad, pero la compatibilidad operativa prioriza el basename original. |
| 2026-06-12 | Revision de cumplimiento por lineas del proyecto | El usuario pidio asegurar si estaban todas las lineas y si el proyecto cumplia lo requerido. | Se registra que, como proyecto preliminar academico, estan cubiertas agua, fangos/lodos, gas/energia/biogas, olores, implantacion, planos/perfiles, Excel, memoria y defensa. La validacion funcional GPS-X queda separada del cumplimiento documental/estructural. |
| 2026-06-12 | Alcance real de biogas y olores en GPS-X 8.0 | Se reviso el paquete GPS-X y se confirmo que el digestor contiene salida/parametros de gas, pero no una linea completa de gas/energia ni olores. | GPS-X se usa para tren agregado de proceso con digestion y dato de biogas; la red completa de gas/energia y la linea de olores se mantienen en Excel, memoria, DXF/PDF, perfiles y gemelo digital. |
| 2026-06-12 | Biogas es requisito teorico del proyecto | Las fuentes de evaluacion y desglose piden linea de gas, aprovechamiento de biogases y justificacion de energia/potencia aprovechada del biogas de digestion. | La linea de gas/biogas es obligatoria en los entregables del proyecto; no debe eliminarse aunque GPS-X no la modele como tren energetico completo. |
| 2026-06-12 | Adopcion de Qpunta por coeficiente 1,30 | El usuario indico usar el caudal de Vero: 64.800*1,3 = 84.240 m3/d. El Anejo 1 conserva tambien una tabla redondeada de 86.400 m3/d. | El modelo usa 84.240 m3/d para dimensionamiento seco y conserva 86.400 m3/d como contraste trazable en hojas de caudales, trazabilidad y defensa. |
| 2026-06-12 | Integracion de trazabilidad y defensa rescatada de la planilla de Vero | La defensa necesita que cada ecuacion importante indique origen, fuente/PDF y hoja de uso. | `EDAR_Modelo_Simplificado_v0.xlsx` incorpora `05_Trazabilidad_Formulas_Fuente`, `06_Auditoria_Trazabilidad`, `73_Riesgos_Cautelas` y `95_Puente_GPSX`. |
| 2026-06-12 | MBR conservador por espacio y operacion N-1 | La parcela es escasa y el usuario pidio mantener una solucion defendible; Vero usaba un criterio mas conservador. | Se adopta MBR a 20 LMH, 8 trenes 7+1, 175.500 m2 de membrana y flujo N-1 de 22,86 LMH. La huella se mantiene como 4 canales dobles compactos. |
| 2026-06-12 | Separacion hidraulica de lluvia y biologico/MBR | Vero explicaba que el caudal de lluvia va a cabecera/primario y no al BMR/MBR completo; el usuario pidio implementarlo. | Se agrega tanque de tormenta de 3.645 m3, con 6 m utiles para reducir huella. El MBR se dimensiona con punta seca; la lluvia completa no entra al MBR. |
| 2026-06-12 | Criterio defendible de fosforo, lodos y biogas | El usuario pidio guiarse por lo mas defendible en biogas/lodos y no generar diferencias absurdas con GPS-X. | Se adopta EBPR 60% con FeCl3 40% de respaldo, rendimiento de fango biologico 0,65, fango quimico 5 kgMS/kgP y torta al 28%. Resultan digestores 9.117 m3 y biogas 6.799 m3/d. |
| 2026-06-12 | Bombas y perdidas como soporte GPS-X trazable externo | El usuario pidio indicar perdidas y bombas en GPS-X, pero la plantilla `06mbr1` no representa la red hidraulica completa ni sustituye el calculo Excel/DXF de cotas, codos y perfiles. | El paquete GPS-X incorpora `inputs/hydraulic_pumps_and_losses.csv`, filas B1/B5 en `gpsx_parameter_map.csv`, README, mapeo, checklist y resumen JSON. B1/B5 quedan documentadas para comparar energia/cotas; solo se convertiran en unidades GPS-X si se mapean manualmente bombas explicitas y se registra el cambio. |
| 2026-06-12 | Sintonizacion inicial del MBR GPS-X con volumen y purga de diseno | La primera corrida estacionaria en GUI uso el volumen MBR de plantilla (500 m3), produjo SRT bajo, MLSS irreal y no nitrifico, por lo que no representaba el Excel simplificado. | `scripts/generate_gpsx_package.py` inyecta en el layout `Cvmcon&o=20020`, `Cmemarea&o=175500`, `Ctankarea&o=3640`, modo MBR de volumen/calculo y purga inicial `Cqcon&p=417,083333` con `Cqmax&p=1251,25` por linea. Se debe reabrir el layout compatible y repetir la corrida. |
| 2026-06-12 | Perfil redox MBR 1 anoxico + 3 aerobicos en GPS-X | El usuario propuso usar el criterio de ejercicios previos: primera zona anoxica y las siguientes aerobicas. Frente a 2 anoxicas + 2 aerobicas, aumenta el volumen aerobico disponible y deberia favorecer la nitrificacion inicial. | El layout GPS-X queda con `1setpsol&o=0.0F2.0F2.0F2.0F` y `1fraircon&o=0.0F0.5F0.5F0.5F`. Tras correr, se debe revisar NH4, NO3/TN, MLSS, SRT y WAS; si nitrifica pero queda mucho nitrato, ajustar recirculacion interna o volumen anoxico. |
| 2026-06-12 | Biomasa inicial defendible en GPS-X antes de modificar el resto | La corrida dinamica con el MBR corregido seguia mostrando MLSS del orden de 150 mg/L y NH4 alto, coherente con un arranque sin biomasa. El usuario pidio usar bibliografia existente para defender la semilla y probar primero GPS-X. | El layout compatible incluye en `!INZ` una biomasa inicial Mantis2 con MLSS objetivo 7.000 mg/L y MLVSS 5.250 mg/L: `1ixbhl`, `1ixbail`, `1ixbaal`, `1ixil`, `1ixul`, `1ixsl`, `1ixiil` y solubles iniciales. La base bibliografica queda trazada a `BIO_MLSS`, manual de fangos activos y referencia BRM/MBR con SSLM 5,2-10 g/L. Excel/memoria no se modifican hasta validar la corrida. |
| 2026-06-13 | Recirculacion interna MBR Ri=3 aplicada dentro de GPS-X | Al comparar con `D:\projects\Asus\gps-x_9` y con `C:/GPS-X80/mantis2lib/mantimbr.con`, se confirmo que `1flowrate&o` es recirculacion interna, no aireacion. El modelo anterior estaba cargando ahi el aire preliminar, dejando Ri muy bajo. | `scripts/generate_gpsx_package.py` carga `1flowrate&o(1)=194.400 m3/d`, `from tank 4 -> to tank 1`, fracciones 0,25/0,25/0,25/0,25, y B5 como `Cintpumphead` + `Cintpumpheadloss`. Los outputs de efluente se amplian con NH4, P soluble y P total. Correr primero el layout Ri3 antes de tocar Excel/memoria. |
| 2026-06-13 | Mantener MBR GPS-X con 4 zonas internas y rotulos en español | La plantilla `06mbr1` usa el modelo `mantimbr.con`, que fija `n&o=4`; pasar a 5 zonas exige cambiar de arquitectura o plantilla. El usuario pidio entender y visualizar mejor el modelo. | El layout Ri3 queda con `Reactor MBR 4 zonas`, zona 1 anoxica y zonas 2-4 aerobicas. Los equipos y pestañas se renombran en español y los outputs quedan como `Afluente`, `Primario`, `Reactor MBR`, `Vertido`, `Perfiles MBR`, `Membranas` y `Calidad efluente`. |
| 2026-06-13 | Separar objetivos oficiales de salida de resultados GPS-X preliminares en una ficha Word | El usuario pidio un Word con calidad/caudal de entrada y salida exigidos por el proyecto, y aclaro que `Caso6_Jose_Espinoza.pdf`/`.docx` solo aporta ideas de configuracion del reactor. | Se crea `3informe/base_caudal_calidad_entrada_salida_v0.docx` desde `scripts/generate_base_caudal_calidad_docx.py`. La ficha usa Anejo 1 y Excel simplificado como fuentes oficiales, y deja Caso6 solo como idea externa: anoxico inicial, zonas aireadas, SRT y recirculacion interna. |
| 2026-06-13 | Incorporar area y pendiente del terreno a la ficha base del proyecto | El usuario pidio que el Word incluya, ademas de caudal/calidad de entrada y salida, el area y la pendiente del terreno. | `scripts/compute_terrain_stats.py` calcula superficie y pendientes desde `Zona.shp` y `TopoDEM.tif`; `base_caudal_calidad_entrada_salida_v0.docx` incluye area 37.306,95 m2, pendiente global 1,84% hacia S y pendiente local media 9,70%, dejando claro que es dato DEM de anteproyecto. |
| 2026-06-13 | Reconstrucción desde cero (Línea de Agua) en `gpsx8_2` | Corregir el error hidráulico de caudal cero en el efluente final aislando solo la línea de agua primero. | Se crearon archivos de layout limpio en `gpsx8_2` copiados del original `06mbr1` y se modificó `generate_water_line.py`. |
| 2026-06-13 | Reemplazo del Decantador Primario por FeCl3 (`metaladd`) | Evitar el decantador primario en la primera fase para asegurar disponibilidad de carbono para desnitrificación. | Se sustituyó el bloque del primario (`simple1d`) en la coordenada `15 18` por un bloque `metaladd` (dosis inicial 0) actuando como pasante. |
| 2026-06-13 | Conexión directa de WAS a sumidero de lodos | Mantener MBR sin puertos desconectados ("no hay puertos sin conectar en el MBR") y derivar el fango de purga. | Se enlazó el puerto 3 de MBR directamente al bloque `Sludge` en `19 16`, anulando los bloques de espesador, digestor y deshidratación temporalmente. |
| 2026-06-13 | Uso de Caudal Punta y Temperatura Crítica en la Línea de Agua | El usuario solicitó simular el peor caso de diseño: caudal punta de 84.240 m3/d y temperatura de 13 °C. | Se modificó `generate_water_line.py` y `audit_water_line.py` para parametrizar el afluente a 84.240 m3/d, la recirculación interna a 252.720 m3/d (Ri=3) y la temperatura global de simulación a 13 °C. La generación y auditoría concluyeron en CONFORME ESTRUCTURAL. |
| 2026-06-13 | Corrección de sintaxis y auditoría del layout en `gpsx8_2` | El compilador de GPS-X fallaba debido a un espacio en blanco antes de `Xtransfermethod` en el archivo `.lyt` y el auditor daba error por formato. | Se eliminó el espacio en blanco antes de `Xtransfermethod&o` en `generate_water_line.py` y se corrigió el string de búsqueda del flujo en `audit_water_line.py` (de `0.0;` a `0.0!;`). El layout ahora se autocompila correctamente en GPS-X y el auditor arroja resultado CONFORME ESTRUCTURAL. |
| 2026-06-13 | Mapeo de volumen MBR a modo Fixed Volume | El solver estacionario (Trim) de GPS-X no converge con control de nivel dinámico en volumen variable (Xmbrvol=2), dando caudal efluente 0.0 m3/d. | Se cambiaron `Xmbrcalc` y `Xmbrvol` a `1` en `generate_water_line.py` y `generate_gpsx_package.py`. Esto desactiva el lazo de control de nivel dinámico y fija el volumen constante, logrando que el balance hidráulico fluya correctamente y el simulador muestre el caudal real del efluente. |
| 2026-06-13 | Corrección de incompatibilidad (vectores MBR con sufijo F) | Al abrir el archivo en la GUI saltaba un error de incompatibilidad que causaba que GPS-X reseteara el reactor MBR a los valores por defecto (500 m3). | Se identificó que GPS-X requiere que todos los vectores finalicen con `F` (ej: `0.25F0.25F0.25F0.25F!`). Se corrigió el formateador y los auditores en `generate_water_line.py`, `generate_gpsx_package.py` y los auditores correspondientes. El error de incompatibilidad y el reseteo de parámetros al abrir el layout han sido resueltos. |
| 2026-06-13 | Corrección de sintaxis en el archivo .lyt por modificadores inválidos | Los usuarios experimentaban errores de compilación `qcon- = 10.0;` y fallos al cargar el bloque `simple1d` y MBR. | Se identificó que el script usaba el modificador inválido `&s` en lugar de `&p` para parámetros de purga de bombeo, además de atributos no oficiales en MBR. Se limpiaron `generate_water_line.py` y `generate_gpsx_package.py`. |
| 2026-06-13 | Configuración de 5 zonas (1 anaerobia + 4 aeróbicas) en GPS-X | El usuario solicitó cambiar a 5 reactores y establecer una recirculación desde la zona aeróbica 5 a la anaerobia 1. | El bloque `mantimbr.con` está limitado internamente a 4 zonas (`n&o=4`). Se decide que para lograr 5 zonas, se debe incorporar de manera gráfica un tanque anaerobio separado (CSTR/Plug-flow) antes del MBR y conectar la recirculación de forma externa en la interfaz de GPS-X. |
| 2026-06-13 | Variante reproducible `gpsx8_2_tuned_5zone` para resolver NH4 alto | La GUI permitió guardar `Pn&o=5` en `gpsx8_2`, pero el NH4 seguía alto. La captura muestra hidráulica correcta y apunta a SRT baja/purga excesiva, NH4 afluente no explícito y falta de volumen aeróbico, no a un problema primario de KLa. | Se crea una variante separada con Qmedio 64.800 m3/d, `Csnh&o=25`, 5 zonas 20/20/20/20/20, OD 0/2/2/2/2, recirculación interna R5->R1 a Ri=4Q, purga fija `Cqcon&p=800,8 m3/d` para SRT ~25 d y controlador MLSS de purga desactivado. La decisión previa de 5 zonas imposibles queda como cautela para el paquete formal, pero la variante GUI se acepta como prueba funcional. |
| 2026-06-13 | Mantener controlador de OD antes de probar KLa | En `mantimbr.con`, `Xtransfermethod&o=3` significa controlador de OD; los parámetros `klacon&o` solo gobiernan si `Xtransfermethod&o=2`. | No se sube KLa como primera acción. La variante mantiene `Xtransfermethod&o=3`, carga KLa/aire solo como respaldo y la validación funcional debe revisar si el OD real llega a 2 mg/L en zonas 2-5 antes de cambiar de método de oxígeno. |
| 2026-06-13 | Subir SRT objetivo de la variante 5 zonas a 30 d | La revisión teórica de `manual/best` indica que la nitrificación depende fuertemente del SRT, que a baja temperatura conviene margen de seguridad, y que la aireación extendida/MBR puede operar alrededor de 20-30 d. Con NH4 alto y OD en consigna, la primera acción defendible es proteger biomasa nitrificante antes que tocar FeCl3 o línea de fangos. | `generate_water_line.py` calcula ahora WAS con volumen efectivo MBR `Vaerobio + R/(R+1)*Vanoxico = 19.219,2 m3`; queda `Cqcon&p=640,64 m3/d` para SRT ~30 d y `Cqmax&p=1067,73 m3/d` como referencia de SRT ~18 d. `audit_water_line.py` fue actualizado y la variante `gpsx8_2_tuned_5zone` pasa auditoría estructural. |
| 2026-06-13 | Sustituir ruta operativa de 5 zonas por variante estable 4 zonas | El usuario reportó que el archivo tardaba mucho en abrir. La revisión de `C:/GPS-X80/mantis2lib/mantimbr.con` confirma que el MBR nativo usa `n&o=4`; forzar `Pn&o=5` y vectores de 5 posiciones puede ralentizar o desestabilizar la carga. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_fast4zone`, con 4 zonas nativas, OD 0/2/2/2, Ri=4Q desde zona 4 a zona 1, SRT 30 d, `Cqcon&p=633,966667 m3/d` y sin `Pn&o=5`. `gpsx8_2_tuned_5zone` queda como experimento histórico, no como ruta recomendada. |
| 2026-06-13 | Aislar el reactor MBR de 5 zonas antes de agregar unidades auxiliares | El usuario indico que la variante de 4 zonas tampoco era la solucion y pidio volver a 5 reactores, dejando solo el reactor hasta conseguir valores biologicos razonables. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_reactor_only_5zone`: solo Afluente, Reactor MBR 5 zonas, Vertido y Fango WAS. Se retiran temporalmente FeCl3, UV, espesador, digestor, deshidratacion y retornos. Se conserva Qmedio 64.800 m3/d, 13 C, OD 0/2/2/2/2, Ri=4Q desde R5 a R1, SRT 30 d y WAS 640,64 m3/d. |
| 2026-06-13 | Cerrar `Pn&o=5` interno como invalido y pasar a arquitectura externa 1+4 | La GUI de GPS-X queda pegada y Java muestra `ArrayIndexOutOfBoundsException: 5`, confirmando que el bloque MBR nativo no soporta un quinto compartimento interno. | `generate_water_line.py` y `audit_water_line.py` pasan a generar/auditar `gpsx8_2_external5_mbr4_reactor_only`: tanque anoxico externo `mantis2` de 5.005 m3 + MBR nativo de 4 zonas aerobicas de 15.015 m3. Se elimina `Pn&o=5`, se conserva WAS 633,966667 m3/d para SRT 30 d y se documenta Ri=4Q R5->R1 como ajuste GUI pendiente al confirmar puerto de licor mezcla. |
| 2026-06-14 | Preparación de EDAR Silvouta final en GPS-X | El usuario indicó que layout_compat_06mbr1_Ri3 es el único que funciona bien. | Se copió ese layout a layout_EDAR_Silvouta renombrando los archivos a EDAR_Silvouta.* para tener el diseño final listo para correr. |
| 2026-06-14 | Configuracion GUI de la Recirculacion Interna (MLR) en el MBR | El usuario experimentaba flujo cero al intentar retornar lodos desde el reactor de membranas a la cabecera mediante tuberias fisicas o tablas internas fallidas. | Se decide priorizar la tabla nativa Internal Flow Distribution del bloque MBR, tal como dicta el manual oficial, y no fabricar bucles fisicos externos con bombas o splitters, para mantener la limpieza del layout. Se instruyo al usuario para solucionar los errores de sintaxis (qrecirculacion) introducidos en los campos de control. |
| 2026-06-14 | Crear `layout_EDAR_Silvouta_v2` con basename compatible `06mbr1` | El layout manual `layout_EDAR_Silvouta` mezclaba basename renombrado, no incluia `.prx` y forzaba `Pn&o=5`/vectores de 5 zonas en un MBR nativo limitado a 4 zonas. El usuario confirmo Q adoptado 84.240 m3/d. | La v2 se genera desde `layout_compat_06mbr1`, se abre como `layout_EDAR_Silvouta_v2/06mbr1.lyt`, mantiene 4 zonas internas, Q=84.240 m3/d, T=13 C, Ri=3 y WAS fijo preliminar para SRT aproximada de 20 d. Se documenta el diagnostico de nitrogeno y se audita con `audit_layout_edar_silvouta_v2.py`. |
| 2026-06-14 | Usar `Membrane Bioreactor (MBR)`/`06mbr1` y no otros MBR para Silvouta v3 | La paleta GPS-X contiene `Membrane Bioreactor (MBR)`, `Completely-Mixed MBR` y `Anaerobic MBR`; el usuario pidio usar solo el que sirva mejor. Para bajar N se necesitan zonas redox y recirculacion interna. | Se elige `Membrane Bioreactor (MBR)`/plantilla `06mbr1`. Se descarta `Completely-Mixed MBR` para nitrogeno por no representar zonas, y se descarta `Anaerobic MBR` por no aplicar a nitrificacion municipal. La v3 mantiene 4 zonas, KLa explicito, Ri=3,5 y retornos de fangos cerrados a cabecera. |
| 2026-06-14 | Reincorporar FeCl3 como variante separada `layout_EDAR_Silvouta_v4_quimica` | El layout manual anterior contenia un `metaladd` valido para FeCl3, pero tambien arrastraba `Pn&o=5` y vectores de 5 zonas inestables. El control quimico sirve para fosforo y no debe confundirse con la causa del NH4 alto. | La v4 se genera desde la base v3 y agrega `metaladd` entre `peff` y MBR (`Primario -> FeCl3 -> MBR`), con `Xchemical=4`, `Xdosmode=2` y dosis activa 15 gFe/m3. Se documenta que 15 gFe/m3 es un caso fuerte; el Excel con EBPR 60% equivale a unos 5 gFe/m3, por lo que la calibracion final debe comparar 0/5/15 gFe/m3 tras estabilizar la biologia. |
| 2026-06-14 | Adopción de configuración GPS-X punta funcional como base de sincronización | La ultima corrida funcional en GUI del usuario valida el escenario punta con Q=84.240 m3/d y efluente conforme; era necesario separar la fuente final de las variantes historicas de depuracion. | Se adopta como layout funcional final `D:\projects\DSTA\gps_x\DSTA_Silvouta_MBR_GPSX8\layout_EDAR_Silvouta_Final` (`PlantaEDAR.lyt`). Se adopta MBR nativo GPS-X 8.0 de 4 zonas y se descarta `Pn&o=5` interno. La recirculacion interna queda 4->1 = 497.220 m3/d en `Internal Flow Distribution`, WAS = 600 m3/d, FeCl3 = 15 gFe/m3 y SRT = 30,76 d. El escenario punta cumple objetivos de efluente y Excel/CAD/SVG/3D/informe deberan sincronizarse con estos resultados. Los layouts anteriores no se borran; se mantienen como historial tecnico hasta que el usuario autorice archivado o limpieza. |
| 2026-06-14 | Creación de Excel Modular por equipos v1_COMPLETO | Necesidad de un predimensionamiento estático trazable por unidad de equipo, separado de la simulación funcional de GPS-X. | Se creó `EDAR_Modelo_Modular_Tipico_GPSX_v1_COMPLETO.xlsx` con 17 hojas enlazadas. Sirve como base para el diseño civil y electromecánico. |
| 2026-06-14 | Implantación GIS-First (EPSG:25829) | Asegurar que la ubicación de equipos responda a la topografía real y límites legales antes de generar planos CAD. | Se generaron capas en `1gis/EDAR_Modular_GPSX_v1/` (GPKG, SHP, KML) con huellas de equipos, cotas y pendientes extraídas del DEM. |
| 2026-06-14 | Word como Centro de Información del Proyecto | Consolidar objetivos, resultados validados y compendio de ecuaciones en un único documento de referencia técnica. | Se generó `base_caudal_calidad_entrada_salida_v1_centro_info.docx` incluyendo el compendio de fórmulas de diseño estático. |
| 2026-06-14 | Crear `EDAR_Modelo_Simplificado_v1_GPSX.xlsx` como puente Excel entre GPS-X y entregables | El v0 debia mantenerse intacto y los resultados funcionales GPS-X necesitaban una capa Excel trazable antes de actualizar CAD, SVG, visor 3D o informe. | Se crea `2planilla/EDAR_Modelo_Simplificado_v1_GPSX.xlsx` desde v0 mediante `scripts/update_excel_gpsx_v1.py`. La v1 incorpora fuente de datos, resumen de cumplimiento, linea de agua GPS-X, fangos GPS-X, biogas pendiente de reconciliacion, olores preliminares, defensa y puente de entregables. La v1 es el puente de trabajo para las fases siguientes; v0 queda como respaldo intacto y no se actualizan aun CAD/SVG/3D/informe. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v1.xlsx` para defensa academica | El Excel maestro GPS-X v1 es util como puente tecnico, pero para defensa conviene una version reducida, legible y centrada en etapas, variables, ecuaciones, resultados y preguntas esperables. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v1.xlsx` mediante `scripts/create_excel_lite_gpsx_v1.py`, con solo 8 hojas y validacion contra `_control_proyecto/fuente_oficial_gpsx.yaml`. No sustituye al modelo tecnico completo, no modifica `EDAR_Modelo_Simplificado_v1_GPSX.xlsx`, mantiene biogas como pendiente de reconciliacion y olores como calculo preliminar externo a GPS-X. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v2_formulas.xlsx` con formulas activas | La v1 lite era legible para defensa, pero algunas salidas derivadas estaban pegadas como valores y no como calculos auditables. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v2_formulas.xlsx` desde la v1 mediante `scripts/create_excel_lite_gpsx_v2_formulas.py`. La v2 mantiene las 8 hojas, agrega referencias a variables base, `FORMULATEXT`, formulas de cumplimiento, fangos/camiones, biogas preparado y olores por foco. No modifica la v1 original ni actualiza CAD, SVG, visor 3D, Word/PDF o informe. |
| 2026-06-14 | Crear `EDAR_Modelo_Lite_GPSX_v3_ecuaciones.xlsx` con criterio estricto de ecuaciones | La revision del usuario exige que solo los datos iniciales queden como valores y que todo resultado derivado se obtenga por ecuacion visible, dejando faltantes editables para completar calculos al introducir datos. | Se crea `2planilla/EDAR_Modelo_Lite_GPSX_v3_ecuaciones.xlsx` desde la v2 mediante `scripts/create_excel_lite_gpsx_v3_ecuaciones.py`. La v3 clasifica `02_Variables` por tipo de dato, convierte derivados a formulas activas con columnas de ecuacion/FORMULATEXT, deja biogas/eficiencia como entradas faltantes y valida que v1/v2 no cambien. No actualiza CAD, SVG, visor 3D, Word/PDF ni informe. |
| 2026-06-14 | Crear informe de alcance de implantacion y matriz de equipos antes del Excel modular | El usuario pidio decidir que equipos se pueden/deben incluir, separando lo minimo, lo GPS-X, lo externo a GPS-X, lo recomendable, lo opcional y lo que no conviene modelar para defensa academica. | Se crea `3informe/informe_alcance_implantacion_y_equipos_v1.md` y `_control_proyecto/matriz_equipos_implantacion.csv`. El informe fija el minimo defendible, el maximo razonable, la evaluacion espacial y las hojas recomendadas para un Excel modular de equipos, sin modificar Excel, CAD/DXF, SVG/Inkscape, visor 3D, Word/PDF, informe final ni layouts GPS-X. |
| 2026-06-14 | Actualizar el diagrama SVG/Inkscape con la fuente GPS-X final | El diagrama de flujo seguia representando el modelo simplificado previo y la Fase E del plan pedia sincronizar SVG/Inkscape tras validar GPS-X. | `scripts/generate_process_flow_svg.py` pasa a leer `_control_proyecto/diccionario_variables_edar.csv` y regenera `0diagrama/diagrama_flujo_edar_silvouta_simplificado_v1.svg` con Qpunta 84.240 m3/d, MBR 4 zonas, recirculacion 4->1, resultados de efluente, fangos/camiones y pendientes de biogas/olores. No actualiza CAD/DXF, visor 3D ni informe final. |
| 2026-06-15 | Preparar copia GPS-X outputs plus sin editar archivos internos automaticamente | El usuario pidio extraer variables faltantes y preparar displays/plots, pero el formato `.lyt`/`.sim` de GPS-X es propietario y una reescritura textual puede romper el layout funcional validado. | Se crea/verifica `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS`, se genera `outputs_extraidos_gpsx_plus/` con inventario de variables, outputs encontrados, pendientes, diccionario de displays/plots, guia manual y reporte; se registra la copia en `_control_proyecto/registro_copia_gpsx_outputs_plus.md`. Los displays/plots D01-D15 y P01-P10 se deben crear/exportar desde la GUI GPS-X sobre la copia. |
| 2026-06-15 | Usar `doci_Eflu` como enlace FeCl3 -> MBR en la copia OUTPUTS_PLUS | GPS-X guardo el enlace de salida de FeCl3 con un nombre distinto, pero el MBR conservaba `Spropvar=fecl3`, provocando el error de compilacion `qfecl3` indefinido. El usuario pidio conservar el nombre `doci_Eflu`. | `scripts/repair_gpsx_outputs_plus_qfecl3.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: salida FeCl3, entrada MBR y `Spropvar` quedan en `doci_Eflu`; se restaura Qpunta 84.240 m3/d y se genera respaldo/reporte. El layout final original no se modifica. |
| 2026-06-15 | Usar `MBR_Rebose` como nombre del rebose del MBR en la copia OUTPUTS_PLUS | El usuario renombro el rebose del MBR a `MBR_Rebose`, pero GPS-X mantenia una referencia interna antigua `Scvarp=xmbrof`, provocando el error de compilacion `xmbrof` indefinido. | `scripts/repair_gpsx_outputs_plus_mbr_rebose.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: el puerto se conserva como `MBR_Rebose` y el controlador queda como `Scvarp=xMBR_Rebose`. Se genera respaldo/reporte y el layout final original no se modifica. |
| 2026-06-17 | Adoptar `OUTPUTS_PLUS/max` como fuente documental gobernante | El usuario aclaro que la discrepancia se debe a que el layout quedo cerrado con promedio mientras se cargaban/exportaban maximo y promedio, y que todos los entregables deben usar caudal maximo. | `_control_proyecto/fuente_funcional_outputs_plus_max.yaml/json` fija `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/max/plantaedar.xls` como fuente para Excel, diagrama, GIS y documentos. `Promedio/` queda como antecedente no gobernante. |
| 2026-06-17 | Separar Excel completo de origen y Excel simplificado gobernante | La entrega necesita trazabilidad de ecuaciones/fuentes y, a la vez, un libro simple con entradas, formato y resumen que gobierne el resto de documentos. | Se crean `EDAR_Modelo_OUTPUTS_PLUS_ORIGEN_v1.xlsx` y `EDAR_Modelo_OUTPUTS_PLUS_Simplificado_v1.xlsx`. El primero conserva origen, hashes, tablas GPS-X y ecuaciones; el segundo gobierna memoria, diagrama, GIS y futuras actualizaciones de CAD/visor. |
| 2026-06-17 | Regenerar diagrama y poligonos GIS desde el Excel OUTPUTS_PLUS max | Las instalaciones adicionales pedidas por el informe no existen todas como unidades dinamicas en GPS-X y debian incorporarse al flujo documental y espacial. | Se genera `diagrama_flujo_edar_silvouta_outputs_plus_v1.svg` y `1gis/EDAR_Modular_GPSX_v2/` con 29 huellas, 0 equipos fuera de `Zona` y 0 solapes; CAD/DXF, visor 3D e informe quedan como siguientes derivados del Excel simplificado. |
| 2026-06-17 | Incorporar alturas TopoDEM en `equipos_edar` | El usuario pidio que los poligonos de equipos incluyan altura maxima, minima y promedio desde el raster `TopoDEM`. | `equipos_edar.gpkg` incorpora `altura_min_m`, `altura_max_m` y `altura_prom_m` por poligono. Si el SHP original esta bloqueado por QGIS, se entrega `equipos_edar_alturas.shp` con alias compatibles `alt_min_m`, `alt_max_m` y `alt_prom_m`. |
| 2026-06-17 | Reparar variables internas de OUTPUTS_PLUS en copia de dos trenes | GPS-X guardo streams con sufijos `doci_Eflu1/2` y `MBR_Rebose1/2`, pero el primer MBR conservaba referencias internas sin sufijo que podian compilar como variables indefinidas. | `scripts/repair_gpsx_outputs_plus_split_trains.py` actualiza solo `layout_EDAR_Silvouta_Final_OUTPUTS_PLUS/PlantaEDAR.lyt`: `Spropvar=doci_Eflu1`, `Scvarp=xMBR_Rebose1` y `Cqcon&o=84240.0`. Se genera respaldo/reporte y el layout final original no se modifica. |
| 2026-06-18 | Completar el MBR 2 de OUTPUTS_PLUS sin cambiar la logica activo/reserva | El usuario aclaro que el reparto `Cfr&in&to&o1=1.0` es intencional porque una linea puede quedar en mantenimiento y debe activarse la otra; el fallo estaba en que el MBR 2 no tenia los parametros internos completos del MBR 1. | `scripts/repair_gpsx_outputs_plus_line2_mbr.py` copia al MBR 2 area de membranas, control MLSS/purga, inicializacion biologica y DVA del MBR 1, adaptando `Spropvar=doci_Eflu2` y `Scvarp=xMBR_Rebose2`. No cambia el divisor de caudal y no modifica `layout_EDAR_Silvouta_Final`. |
| 2026-06-18 | Crear carpeta `apoyo/` para trabajo auxiliar con Gemini | El usuario pidio ahorrar tokens de OpenAI delegando tareas pesadas a Gemini cuando sea conveniente. | `apoyo/` queda como zona no oficial de intercambio: `pedidos_gemini/` contiene instrucciones especificas, `salidas_gemini/` contiene respuestas, y `INSTRUCCIONES_GEMINI.md` fija que Gemini no modifica fuentes oficiales ni sustituye los Markdown de control. |
| 2026-06-19 | Reparación de tuberías candidatas v2 | Corregir conexiones para que conecten a vértices exteriores sin penetrar polígonos, y solucionar NoData del DEM en Z | Se generaron conexiones ortogonales en GPKG/SHP conectadas a esquinas, se calcularon codos, longitudes 3D y bombeo preliminar con fallback robusto a cota de equipos. |
| 2026-06-19 | No integrar el candidato Qmedio v2 hasta reparar auditoria Codex | La auditoria independiente detecto que v2 aun tiene tramos diagonales en conexiones exteriores, intersecciones con poligonos como segmentos, codos subestimados y una diferencia de superficie Excel/GIS por `A11_VERTIDO`. | Se crea `apoyo/pedidos_gemini/pedido_20260619_reparar_candidato_qmedio_v3_post_auditoria_codex.md`; la proxima salida debe ser una carpeta candidata v3 y no debe tocar archivos oficiales ni Markdown de control. |
| 2026-06-19 | Aceptar v3 como candidato GIS/SVG preferente, no integrado aun | La auditoria Codex de v3 confirma geometria de tuberias conforme: ortogonalidad, vertices, ausencia de intersecciones con poligonos, longitudes 3D y conteo de codos. Queda solo una cautela documental sobre el metodo de conteo de cruces XY entre lineas. | `candidato_20260619_qmedio_svg_gis_tuberias_v3` queda como candidato preferente para promocion oficial cuando el usuario lo apruebe; antes de usar la estadistica de cruces en memoria/planos se debe aclarar 27 pares Gemini frente a 33 pares Shapely/Codex con interseccion no vacia. |
| 2026-06-19 | Generar QGZ de revision antes de promocion oficial | El usuario quiere revisar visualmente el candidato v3 en QGIS antes de integrarlo en las carpetas oficiales. | Se genera `qgis_revision_candidato_qmedio_v3.qgz` dentro de la carpeta candidata, con rutas relativas y capas activadas de DEM, Zona, equipos, tuberias 3D y centroides. Este QGZ es de revision, no una promocion oficial. |
| 2026-06-19 | No integrar v3 tras revisión visual del usuario | El usuario observó que algunas tuberías salían del área de estudio de la meandro `Zona.shp`. | Se decidió rechazar el candidato v3 y generar una versión v4 que mantenga todas las geometrías al 100% dentro del polígono de Zona. |
| 2026-06-19 | Generar candidato v4 con restricciones de área estrictas y dos líneas | Asegurar que ninguna tubería salga de `Zona.shp`, documentar la configuración de doble línea (L1 activa, L2 reserva) en atributos GIS, y actualizar nombres en español en el SVG. | Se inyectó control estricto de límites en el ruteador (`route_pipes_v5.py`) y 10 atributos de doble línea en equipos, y se tradujeron etiquetas del SVG a español con acentos correctos. La auditoría final reporta 0 elementos fuera de Zona. |
| 2026-06-19 | División física en dos líneas paralelas en GIS (Candidato v5) | El usuario solicitó representar de forma fidedigna el doble tren paralelo de la planta en el GIS, tal como se muestra en el layout GPS-X8. | Se modificó el ruteador (`route_pipes_v6.py`) para dividir físicamente los polígonos de decantadores, reactores MBR y UV en unidades independientes y trazar tuberías paralelas independientes (L1 activa, L2 reserva). Se corrigió la interpolación DEM y se compiló el proyecto QGIS de revisión v5. |
| 2026-06-19 | Promoción de la Alternativa Paralela (Candidato v5) y Compilación de Memoria Word | Establecer el trazado del Candidato v5 como versión oficial georreferenciada y asegurar la consistencia del informe Word. | Se ejecutó `promote_candidate_v5.py` para promover el Candidato v5 (capas GIS oficiales en `1gis/EDAR_Modular_GPSX_v2`, diagramas SVG y QGIS de revisión). Se compiló la memoria oficial [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx) con la justificación del reactor MBR optimizado a $5,5\text{ m}$ de profundidad, el cual reduce la huella a $4.550\text{ m}^2$ por reactor y garantiza contención total con cero solapes. |
| 2026-06-19 | Reparar solo la errata de superficie MBR en el Excel Qmedio oficial | La revision conjunta GPS-X8/Excel mostro que `07_MBR_activo!D11 = 3640 m2` es el area del tanque final por reactor MBR, mientras que `20020 m3` es volumen y no debe dividirse por 3 m para gobernar la superficie de construccion. | `02_Areas_construccion!C14` pasa a referenciar `07_MBR_activo!D11`, con factor `1.25` y area de implantacion `4550 m2` por reactor. Se crea `scripts/repair_qmedio_excel_mbr_area_errata.py` y no se alteran otros parametros del libro. |
| 2026-06-19 | Generar QGZ final corregido desde Excel/GPS-X8 sin seguir modificando otros oficiales | El usuario pidio corregir la errata del Excel y armar el QGZ final; para limitar el riesgo no se siguio tocando GPS-X, SVG oficial, CAD, visor ni memoria Word. | Se crea `apoyo/salidas_gemini/candidatos/candidato_20260619_qmedio_final_excel_corregido/` y el QGZ raiz `qgis_revision_EDAR_Qmedio_final_excel_corregido.qgz`. El GIS representa 2 decantadores y 2 MBR a escala de area corregida, mantiene todo dentro de `Zona`, rutas ortogonales, atributos Z/longitudes/codos/bombeo, y deja v4/v5 como antecedentes a revisar antes de cualquier promocion oficial posterior. |
| 2026-06-19 | Corrección de acople de biogás en planos y perfiles CAD | El auditor detectó un desacople de $2.83\text{ m}$ en la ruta `GAS-ANTORCHA` porque las coordenadas del centro de la planta de biogás estaban cableadas duras. | Se modificó `scripts/generate_profiles_plans.py` para leer dinámicamente el centroide de `G_SISTEMA_BIOGAS` desde `equipos_edar.gpkg`. Se ajustó la cota de salida a $+7.0\text{ m}$ sobre el terreno, y el auditor de planos ahora devuelve `CONFORME`. |
| 2026-06-19 | Integración de datos de tuberías y elevaciones de GIS en Excel | El usuario requirió inyectar los datos reales de longitud, codos, elevaciones y bombeos del GIS final corregido en el Excel oficial. | Se ejecutó `python scripts/integrate_hydraulics_to_excel.py` actualizando y recalculando los datos de las 35 tuberías y 17 bombeos en la hoja `04_Bombeo_tuberias` de `EDAR_Silvouta_Qmedio_linea_activa_ecuaciones_v9_revisado.xlsx`. |
| 2026-06-19 | Regeneración de Memoria Word Unificada | Mantener la total consistencia numérica de las cotas y alturas del análisis con el Word compilado. | Se re-ejecutó `python scripts/generate_word_informe.py` generando con éxito el reporte final unificado [MEMORIA_Y_ANEJOS_v1.docx](file:///d:/projects/DSTA/3informe/MEMORIA_Y_ANEJOS_v1.docx). |
| 2026-06-19 | Separación de plantas/perfiles y simplificación topográfica | El usuario solicitó separar plantas y perfiles (el formato 1x2 anterior los deformaba y hacía muy pequeños) y simplificar la topografía de los perfiles. | Se modificaron los scripts para renderizar plantas y perfiles de agua y fango en páginas independientes A3 del PDF (resultando en 7 páginas totales). Se implementó interpolación lineal en `sample_route` para generar perfiles de terreno y tuberías continuos entre nodos, eliminando el ruido del DEM. El auditor fue actualizado y concluyó en CONFORME. |
| 2026-06-19 | Eliminar etiquetas de nodos intermedios y cotas de equipos en perfiles | El usuario pidió no nombrar "elevación por elevación" en los perfiles porque se genera una mancha de textos ilegible; solo deben quedar las etiquetas de identificación de la infraestructura (equipos). | Pendiente de aplicar: eliminar `ax.text(node.code)` en `draw_profile` (PDF, línea 1015) y `text(node.code)` en `draw_profiles_dxf` (DXF, línea 788). Simplificar la etiqueta del bloque de equipos en PDF (línea 1028) para mostrar solo `eq.code` sin la cota `({eq.coronacion:.1f} m)`. En DXF (línea 801) ya solo muestra `eq.code`. Después regenerar y auditar secuencialmente. |
| 2026-06-19 | Representar perfiles como cadena hidráulica con HGL y bombas | El usuario aclaro que el perfil debe servir para ver equipo tras equipo conectado por tuberias, con bombas asociadas y linea piezometrica inicial para saber donde hay que subir y cuanto. | `scripts/generate_profiles_plans.py` deja de etiquetar vertices intermedios en PDF/DXF, muestra solo equipos principales, incorpora HGL preliminar en naranja y anota saltos de bomba desde `conexiones_sugeridas.gpkg`. Se regeneran DXF/PDF/Excel/informe/manifest y `audit_profiles_plans.py` vuelve a dar `CONFORME`. |
| 2026-06-19 | Usar perfil hidraulico esquematico separado para aguas y fangos en el PDF | El usuario mostro la pagina 742 de `manual/proyecto_real/real.pdf` como referencia y pidio un perfil de aguas y otro de fangos, esquematicos y sin exceso de detalle. | Las paginas 3 y 5 de `4planos/EDAR_Perfiles_Plantas_v0.pdf` pasan a ser laminas hidraulicas de proceso: bloques de equipos, conducciones GIS con longitudes/codos, bombas preliminares y HGL. El Excel `EDAR_Perfiles_Plantas_v0.xlsx` agrega `40_Hidraulico_Aguas` y `41_Hidraulico_Fangos`. El DXF queda regenerado conforme pero mantiene su perfil longitudinal CAD. |
| 2026-06-20 | Corregir perfiles a "cuerda estirada" sobre traza real, manteniendo caracter esquematico | El usuario aclaro que el perfil debe desarrollarse cortando imaginariamente la linea real de aguas/fangos en planta: en planta puede ser serpiente, pero en perfil debe verse recta con PK acumulado, topografia real, equipos secuenciales y tuberias con distancia/pendiente. Tambien confirma que debe seguir siendo una representacion esquematica, con detalle suficiente pero sin exagerar. | `generate_profiles_plans.py` usa muestreo DEM sobre la ruta real, equipos colocados en su PK, tuberias con longitudes/pendientes y bombas/HGL cuando aportan informacion. Se omiten detalles constructivos secundarios y rotulos repetitivos. Decision implementada y validada el 2026-06-20. |
| 2026-06-20 | Usar geometria vertical esquematica relativa al terreno solo en las laminas de perfil | Las cotas antiguas de bloques producian una lectura donde casi toda la linea de agua parecia enterrada y desconectada de la topografia, aunque el objetivo es un esquema de anteproyecto. | El PDF/DXF coloca cada equipo en su PK real y conserva su altura relativa, pero relaciona graficamente la extension vertical con el DEM y conecta el bloque a la conduccion. Esta transformacion es solo visual: no sustituye cotas numericas GIS/Excel ni el calculo hidraulico final. |
| 2026-06-20 | Cerrar la primera version del perfil por cuerda estirada | Se implemento y reviso el criterio confirmado por el usuario para agua y fangos. | Se regeneran PDF, DXF, Excel, informe y manifiesto. Las tuberias principales muestran `L` e `i`; bombas y HGL se identifican sin duplicar textos. `audit_profiles_plans.py` devuelve `CONFORME` y las paginas 3 y 5 quedan revisadas visualmente. |
| 2026-06-21 | División horizontal del perfil desarrollado en dos subplots | Las infraestructuras se veían extremadamente delgadas debido a la gran diferencia entre el rango horizontal (330 m) y vertical (12 m) en un solo gráfico. | Se divide la página de perfiles de la línea de agua y fangos en dos secciones (0-200m y 200m-final), duplicando la escala física en el eje X para mejorar la visibilidad. Se añade una gestión de errores de archivo bloqueado (PermissionError) con salida alternativa. |
| 2026-06-21 | Unificación de perfil (sin partir), espaciado de etiquetas y leyendas | El usuario solicitó no partir el perfil si cabe entero, evitar el traslape de textos y contar con una leyenda clara para las líneas. | Se eliminó la partición en subplots, se implementó un algoritmo de relajación 1D para espaciar etiquetas horizontalmente (con líneas de directriz diagonales) y se inyectaron leyendas explícitas con nombres en español. |
| 2026-06-21 | Escala 1:1 en ejes X e Y y compresión extrema tipo acordeón | El usuario solicitó mantener la misma escala física en ambos ejes (X e Y) para evitar deformaciones verticales y comprimir las tuberías como acordeón. | Se cambió la relación de aspecto del gráfico a 'equal', se ajustó la compresión de tramos de tuberías al 5% (compresión agresiva tipo acordeón) y se regeneró el PDF de planos/perfiles. |
| 2026-06-21 | Corrección de signo de pendiente de gravedad y espaciado de etiquetas | Evitar el traslape de las etiquetas de tuberías comprimidas en acordeón y corregir el signo positivo de las pendientes por gravedad. | Se re-ubicó `space_out_1d` a nivel de módulo en `generate_profiles_plans.py`. Se implementó relajación horizontal 1D de etiquetas con líneas guía punteadas en PDF y DXF. Se forzó signo negativo (`slope = -abs(slope)`) en todas las conducciones de gravedad en planos, Excel y reportes. La auditoría volvió a dar `CONFORME`. |
| 2026-06-21 | Partición horizontal en 2 subplots, recálculo hidráulico y alternancia vertical de etiquetas | El usuario solicitó partir la hoja por la mitad horizontalmente para estirar el gráfico a escala 1:1, ampliar el tamaño de los equipos y evitar traslapes de textos de forma definitiva. | Se dividieron las páginas de perfiles en dos subplots apilados (superior: PK 0 a la mitad, inferior: mitad a final), duplicando la escala visual del dibujo. Se recalcularon las cotas Z de conexiones y nodos de ruta en base a `COTA_REF` (consistente con las soleras reales), logrando que los tramos de gravedad desciendan físicamente (pendiente negativa real) y solo suban en impulsiones. Se alternaron las etiquetas arriba/abajo del tubo relajándolas de forma separada. La auditoría dio CONFORME. |
| 2026-06-21 | Estiramiento horizontal (compresión 0.45) y optimización de márgenes en subplots del PDF | El usuario solicitó estirar la escala en ambos lados del perfil dividido para llenar la hoja y evitar textos pequeños o traslapados. | Se incrementó el factor de compresión de acordeón a 0.45 y se redimensionaron las cajas de subplots en el PDF a 90% de ancho y 36% de alto ([0.05, 0.48, 0.90, 0.36] y [0.05, 0.09, 0.90, 0.36]), maximizando el espacio de dibujo y ampliando la escala física resultante sin comprometer el aspecto 1:1 ('equal'). La auditoría dio CONFORME. |
| 2026-06-21 | Sustituir definitivamente el acordeon por PK horizontal lineal en los perfiles PDF | La revision visual detecto que una transformacion no lineal del PK podia inducir una lectura falsa de distancias y que las flechas rectas duplicaban la conduccion continua. | Agua y fangos se mantienen en dos paneles, pero cada panel usa PK real lineal. La vertical queda declarada como esquematica; cada tramo se representa una sola vez y el sentido se marca sobre esa conduccion. Las plantas PDF omiten puntos en vertices y la auditoria final vuelve a dar `CONFORME`. Esta decision sustituye las decisiones de compresion tipo acordeon como regla vigente. |
| 2026-06-21 | Usar las plantas de agua y fangos como mapas de orientacion de sus perfiles | El perfil desarrollado necesita una referencia espacial clara para entender donde comienza, por donde discurre y donde termina dentro de la implantacion. | Las hojas 2 y 4 resaltan la ruta principal sobre la planta, con halo, flechas de sentido, inicio, fin y PK total. El paquete conserva 7 hojas: mapa general; planta+perfil de agua; planta+perfil de fangos; biogas y olores como agregados. |
| 2026-06-21 | Generar dos Excel: standalone hidraulico (Phase 1) y v10 con Qpunta (Phase 2) desde GIS | El usuario solicito completar la etapa pendiente de Excel final hidraulica usando `conexiones_sugeridas.gpkg` como fuente unica y el v9 como plantilla para Phase 2. | `scripts/_generate_phase1.py` lee 35 conexiones GIS, calcula Swamee-Jain/Darcy-Weisbach, y genera: (1) `EDAR_Hidraulica_Bombeo_Tuberias_v1.xlsx` con 5 hojas standalone; (2) `EDAR_Silvouta_Qmedio_linea_activa_v10_bombas.xlsx` con 26 hojas (23 originales + 3 nuevas), `04_Bombeo_tuberias` ampliada con 7 columnas Qpunta activas. |
| 2026-06-21 | Usar Swamee-Jain para factor de friccion y Darcy-Weisbach para perdidas de carga | El v9 usaba una aproximacion simplificada `0.02*(L/D)*(V²/2g)` para hf. Para el calculo independiente de Phase 1 se requeria un metodo mas riguroso y trazable. | Se implemento Swamee-Jain: `f = 0.25/[log10(ε/(3.7D) + 5.74/Re^0.9)]²` con ε = 1.5e-6 m (PVC) y ν = 1.0e-6 m²/s. hf = f·(L/D)·(V²/2g). hm = num_codos × 0.5 × V²/2g. |
| 2026-06-21 | Ampliar v9 a v10 con columnas Qpunta en lugar de hoja separada | El usuario pidio no crear una hoja nueva de Qpunta sino ampliar `04_Bombeo_tuberias` con 7 columnas adicionales (W:AC) con formulas activas Excel. | Las columnas Qpunta referencian las celdas existentes: Qpunta diseño = `IF(B="olores", E, E*1.3)`, velocidad/hf/hm/HMT/potencia Qpunta recalculan con el Qpunta correspondiente. Las formulas diferencian olores (factor 1.2, /3600) de agua (factor 1.3, /86400). |
| 2026-06-21 | Diametros internos por tipo de linea con overrides puntuales | El GIS no contiene diametros; se necesitaba una asignacion consistente con criterios de ingenieria para el calculo hidraulico. | agua=0.9m, agua secundaria=0.6m (overrides: A05→A06a/A06b, A08a/A08b→A10), fangos=0.1m, quimicos=0.05m, retornos/biogas=0.15m, olores=0.5m. Valores provisionales hasta definicion de ingenieria de detalle. |
| 2026-06-21 | Correccion de consistencia fisica de caudales especificos en Python | Las bombas de fangos, biogas y quimicos presentaban potencias de megawatts/gigawatts porque el script usaba el caudal total de agua de la planta por error en Python. | Se modificaron los caudales en Python para usar los valores de diseno de cada tramo (sludge 500/750 m³/d, biogas 2407 m³/d, etc.), normalizando las potencias a rangos reales y fisicos. |
| 2026-06-21 | Correccion de diametro de conducto de FeCl3 y recalculo COM en Excel | Los conductos FeCl3 a MBR (T-10 y T-11) tenian 50mm de diametro en v9 pero llevaban el caudal principal de agua, provocando perdidas de 57,000m y velocidad de 379 m/s en Excel. | Se cambio el diametro de T-10/T-11 a 0.6m en Python y se forzo su escritura en la columna N de Excel. Se recalculo el libro v10 mediante COM de Excel en Windows y se audito confirmando cero errores de celdas. |
| 2026-06-21 | Integrar bombas GIS en el gemelo 3D | El geometry.json tenia 2,474 tuberias reales del GPKG pero 0 bombas, pese a que 17 de las 35 conexiones tienen `requiere_bomba='si'` y `altura_bombeo_m` en el GIS. | `generate_digital_twin.py` coloca `add_pump()` en el vertice inicial de cada tuberia con bombeo, con radio/altura proporcionales y color por tipo de linea. Se regenero `geometry.json` con 17 bombas. |
| 2026-06-21 | Eliminar modelo ASM1 del gemelo digital; migrar a datos GPS-X 8.0 | El gemelo digital usaba constantes cineticas ASM1 (`mu_h`, `K_S`, `Y_H`, etc.) de librerias Python propias que no estaban validadas contra GPS-X 8.0. | `simulate_wwtp_dynamics.py` reescrito completamente con perfil horario sinusoidal escalado al punto de diseno GPS-X 8.0 OUTPUTS_PLUS max. `simulation_results.json` regenerado con 24 horas GPS-X 8.0. |
| 2026-06-21 | Desacoplar modelo parametrico Python (`generate_edar_simplified`) del gemelo digital | `generate_digital_twin.py` importaba `base_inputs`, `compute` y `equipment_rows` del modelo parametrico, creando dependencia circular y valores no validados contra GPS-X. | `generate_digital_twin.py` ahora lee directamente del Excel GPS-X 8.0 via `openpyxl` + helper `_read_gpsx8_value()`. `add_simplified_edar()` ya no recibe el parametro `c`. |
| 2026-06-21 | Mejora estetica del visor 3D con renderizado fisicamente correcto | La estetica anterior era basica y poco profesional para defensa de proyecto. | `app.js` usa ACESFilmicToneMapping, HemisphereLight + DirectionalLight, fog, sombras PCFSoft, 5 graficos Chart.js incluyendo energia/biogas. `index.html` usa glassmorphism CSS y variables de color. |
| 2026-06-21 | Representar la calle como camino plano ancho, no como tuberia cilindrica | El usuario indico: "recuerda hacer el calle un camino plano, no una linea". La calle se representaba como polilinea cilindrica tipo tuberia que no reflejaba la realidad fisica de un vial. | Nuevo tipo geometrico `road` en Python (`add_road_segment`, `add_gis_road`) y Three.js (BoxGeometry de 6m ancho × 0.18m espesor, color asfalto, rotado y anclado al DEM). 6 segmentos planos reemplazan los antiguos segmentos pipe-style. |
| 2026-06-21 | Optimizar rendimiento del gemelo 3D eliminando redundancia geometrica | geometry.json pesaba 5.2 MB con 11,907 objetos (76% esferas de junta redundantes). El usuario pregunto si usar KMZ/QGIS y pidio simplificar, eliminar codos y otras mejoras. | RDP (Ramer-Douglas-Peucker, epsilon=1.5m) en polilineas GIS, deduplicacion de juntas (solo en extremos), pipes diagonales directos sin descomposicion ortogonal, celdas de terreno reducidas (step 8→22). geometry.json pasa a 169.8 KB con 379 objetos (-96.7% tamano, -96.8% objetos). InstancedMesh descartado por innecesario a esta escala y porque romperia raycasting/TransformControls. Auditoria actualizada para aceptar ausencia de juntas como optimizacion. |
| 2026-06-21 | Eliminar edicion 3D y reordenar graficos del visor | El modelo GIS es fijo y la edicion (TransformControls, arrastre, guardado/descarga) ya no se necesita. Los graficos estaban en orden que no reflejaba el flujo logico de proceso. | Eliminados de web/: edit-panel HTML, TransformControls.js CDN, instanciacion/listeners de TransformControls, raycaster de seleccion, selectObject/deselectObject/updateUIInputs/onUIInputChange/onSelectedObjectMove/saveLayoutToServer/downloadLayoutAsJSON, y event listeners asociados. Tooltip hover GPS-X conservado (lectura, no edicion). Graficos reordenados: caudal → organico → nitrogeno → proceso → energia. Calle ya era road; alturas GIS ya se leian de equipos_edar.gpkg. No se regenero geometry.json (solo cambios UI). |
| 2026-06-21 | Triangulacion del terreno desde DEM en vez de celdas sueltas | Las celdas de terreno individuales (terrain_cell) generaban muchas geometrias sueltas y no representaban fielmente la topografia real del DEM. El usuario pidio triangular directamente desde el raster. | Nuevo tipo `terrain_mesh` en Python y JS: un solo `BufferGeometry` indexado con ~1.026 vertices coloreados por elevacion (verde→naranja) y ~1.698 indices de triangulos. Muestreo step=6 sobre el DEM con mascara de nodata. Renderizado en Three.js con `vertexColors: true`, `DoubleSide`, opacidad 0.62 y espejado X via `toSceneX()`. Eliminada la funcion `add_terrain_cell()`. |
| 2026-06-21 | Remover graficos Chart.js del visor 3D temporalmente | El usuario pidio eliminar los graficos por ahora para centrarse en la geometria 3D y las etiquetas. | Eliminados de web/: CDN de Chart.js, panel `#charts-panel` con 5 canvas, controles de simulacion (slider, play/pause). Eliminado de app.js: `Chart.register`, `verticalLinePlugin`, `webCharts`, `initCharts()`, `updateChartsHour()`, `loadSimulationData()`, `initSimulationUI()`, variables `simData`/`currentHour`/`isPlaying`/`playInterval`. `fetch` simplificado a solo `buildPlant()`. `getDynamicTooltipContent()` simplificado. |
| 2026-06-21 | Gestion de etiquetas con toggle de leyenda independiente | Las etiquetas tapaban demasiado la visualizacion 3D. El usuario pidio poder ocultarlas con un toggle de leyenda. | Nueva linea `L_ETIQUETAS = "Etiquetas"` en Python. Las 33 etiquetas reasignadas a esta linea. HTML: boton "Etiquetas/Nombres" en la leyenda con color-box blanco. JS: `LINE_LABELS` y `PROCESS_LINES` incluyen `'Etiquetas'`; el sistema de toggle `activeLines`/`updateLineVisibility()` permite ocultar/mostrar todas las etiquetas independientemente. |
| 2026-06-22 | Crear V10 full ecuaciones sin sustituir GPS-X oficial | El profesor observo pocos calculos y ausencia de referencia GPS; el usuario pidio potenciar Excel comparando con GPS-X como fuente oficial. | Se crea `EDAR_Silvouta_Qmedio_linea_activa_v10_full_ecuaciones_GPSX.xlsx`: Excel explica, predimensiona y reconcilia mediante formulas activas, mientras GPS-X 8.0 `OUTPUTS_PLUS/max` conserva la autoridad de simulacion. La comparacion de efluente usa objetivo Excel frente a resultado GPS-X, sin fingir una segunda simulacion biologica. |
| 2026-06-22 | Separar propiedades de agua, fango, biogas y aire en la hidraulica V10 | `v10_bombas` aplicaba la ecuacion de agua a la desodorizacion y producia potencias del orden de 18 MW. | La V10 usa densidad, viscosidad y rugosidad por fluido, Reynolds, Swamee-Jain, Darcy-Weisbach y `rho*g*Q*H/eta`; para gases no incorpora desnivel geodesico como carga estatica. La ventilacion calculada baja a 29,35 kW y queda como anteproyecto pendiente de fabricante. |
| 2026-06-22 | No ocultar la deriva de hash del XLS GPS-X max | El archivo `max/plantaedar.xls` actual no coincide con el SHA-256 registrado en la fuente funcional JSON. | Los valores del JSON de control siguen gobernando la V10, se registra el hash actual `a578c03c...`, y `26_Control_ecuaciones!C10` queda en `REVISAR` hasta reconciliar o reexportar la fuente. |
| 2026-06-22 | Unificar V11 sin renombrar las hojas de proceso existentes | El usuario pidio reunir full, bombas y supermodo ecuaciones, pero las hojas `01-22` ya son contratos usados por formulas y entregables. | La V11 conserva las 23 hojas de proceso y renumera solo los modulos agregados: bombas `23-24`, soporte/GPS-X/control `25-29` y supermodo `30-32`. El resumen unificado se coloca primero sin modificar las V10. |
| 2026-06-22 | Supermodo ecuaciones con formula visible y catalogo enlazado | Una defensa tecnica necesita demostrar que el resultado procede de una ecuacion activa, no de un valor pegado. | Las hojas de proceso, hidraulica, bombas y comparacion muestran `FORMULATEXT` junto al resultado. `30_Catalogo_Formulas` inventaria 3.054 formulas operativas con resultado vivo; mapa y auditoria verifican cobertura y errores sin crear referencias circulares. |
| 2026-06-22 | Excluir mapa y auditoria del catalogo operativo para evitar circularidad | Catalogar formulas que cuentan o auditan el propio catalogo generaria dependencias circulares. | El catalogo cubre el 100% de las formulas operativas previas a las capas auto-referenciales; `31_Mapa_Unificado` y `32_Auditoria_Supermodo` se controlan separadamente. La validacion final confirma 15.402 formulas totales y 0 referencias circulares. |
| 2026-06-22 | Entregar una variante V11 con el formato visual original | El usuario pidio retirar el formato azul y volver al aspecto del libro original. | Se conserva la V11 azul como antecedente y se crea `..._Formato_Original.xlsx` con fondo blanco, texto negro Calibri, bordes negros y graficos grises. El cambio es exclusivamente visual: 34 hojas, 15.402 formulas y resultados permanecen iguales. |
| 2026-06-22 | Reparar referencias vacías en la columna E (GPS-X8) de las hojas de proceso y resumen | Las celdas de columna E vacías hacían que las fórmulas de comparación en la columna F evaluaran a cadena vacía `""` ("apuntar a la nada"). | Se implementaron fórmulas activas en la columna E vinculando las variables calculadas con los datos del modelo correspondientes (`E9*E10`, `E17*E18/100`, `ROUNDUP(E13/20,0)`, `IF(E6<=25,"Cumple","No cumple")`, etc.), eliminando 52 celdas vacías indirectas en la auditoría de celdas seguras y asegurando la coherencia física en el libro de formato original. |
| 2026-06-23 | Merge quirúrgico de `web/app.js` en lugar de sobrescritura total | El manual solo proporcionaba la lógica de UI/datos (~435 líneas: `UNIDADES_DATA`, paneles, raycasting click, exageración vertical, resize) pero no incluía la infraestructura de escena Three.js existente (601 líneas: cámara, renderer, OrbitControls, iluminación, `buildPlant()`, animación de agua). Una sobrescritura habría roto el visor 3D. | Se implementó un merge de 15 bloques (A-O): se preservó la infraestructura de escena (líneas 1-378), se reemplazó el hover `mousemove` por click `raycaster`, se integró `UNIDADES_DATA` (29 equipos), se actualizó `showProfileMode()` para checkboxes `.layer-chk`, se corrigió `resize()` con `getBoundingClientRect()`, y se eliminó `INTERSECTED` en favor de `SELECTED_MESH`. Resultado: 885 líneas unificadas. |
| 2026-06-23 | Implementación delegada a modo Code con especificación precisa de 15 bloques | El modo project-manager solo puede editar archivos `.md`. Los 4 archivos a modificar son `.html`, `.js`, y `.css`. | Se delegó la implementación a modo Code mediante `new_task` con la especificación completa de los 15 bloques de merge, el contenido exacto de cada archivo del manual, y los pasos de verificación. Code mode ejecutó las 5 operaciones (crear dir, 2 creates, 1 overwrite, 1 merge) y verificó `resize()`, selectores, y arranque del servidor. |
| 2026-06-23 | Fondo claro en sprites de etiquetas 3D | El fondo oscuro `rgba(8,14,22,0.78)` de `makeLabelSprite()` se fundía con el canvas `#111820`, haciendo ilegibles los textos. | Se cambió a `rgba(255, 255, 255, 0.88)` con borde `rgba(0, 0, 0, 0.25)` y se añadió `sprite.renderOrder = 999` para que los sprites se dibujen siempre por encima de la geometría opaca. |
| 2026-06-23 | Asignar `mesh.name = "dem_terrain_mesh"` y subir opacidad del terreno | El terreno carecía de `.name`, por lo que `getObjectByName("dem_terrain_mesh")` en `updateVerticalScale()` retornaba `null` y la exageración vertical nunca se aplicaba al DEM. La opacidad 0.62 era demasiado baja sobre fondo oscuro. | Se añadió `mesh.name = "dem_terrain_mesh"` en `buildPlant()` caso `terrain_mesh` y se subió la opacidad por defecto de 0.62 a 0.85. Ahora el slider de exageración vertical afecta al terreno y éste es visible. |
| 2026-06-23 | Luminancia adaptativa en sprites para texto claro → oscuro | Las 23 etiquetas de equipos usan `color=#ffffff` (blanco), invisible sobre el nuevo fondo claro `rgba(255,255,255,0.88)` de los sprites. | Se añadió detección de luminancia en `makeLabelSprite()`: si `lum > 180` (color muy claro), se reemplaza por `#17313b` (texto oscuro del tema). Los colores vivos/oscuros de etiquetas de contexto pasan sin cambio. |
| 2026-06-23 | Eliminar GridHelper, reducir sprites, exageración 2x por defecto | El GridHelper de 900×90 divisiones dominaba la escena; las cajas de texto eran gigantes con texto pequeño (padding 40 px, scale 0.18, altura 1.8×); la topografía de 7.8 m de relieve era invisible a escala 1x desde cámara elevada. | GridHelper eliminado; sprites reducidos (padding 20 px, scale 0.10, altura 1.4×, fuente mínima 36 px); `updateVerticalScale(2)` al iniciar para que el terreno sea visible desde la carga. |
| 2026-06-23 | Respuestas detalladas a Detalles de Evaluación y Examen | El usuario solicitó responder a las preguntas de Detalles de Evaluación y Examen basándose en los parámetros del proyecto. | Se creó el archivo `3informe/RESPUESTAS_EVALUACION_Y_EXAMEN.md` con respuestas estructuradas a las 13 preguntas orales y la justificación de los 14 puntos opcionales (+2.0 pts extra). |
| 2026-06-23 | Encuadre automatico y etiquetas con prioridad en el gemelo 3D | La captura del usuario demostro que una camara fija y 33 sprites siempre visibles hacian ilegible el modelo y ocultaban el panel tecnico. | El visor calcula el encuadre desde los objetos GIS visibles, inicia a 1x y usa colision/prioridad para rotulos. `Presentacion` muestra solo hitos, `Tecnico` habilita candidatos adicionales y malla DEM, y `Limpio` oculta etiquetas. |
| 2026-06-23 | Consistencia numérica y unificación de examen | El usuario solicitó alinear las respuestas del examen con los datos definitivos del Excel V11 y la simulación Max de GPS-X8. | Se actualizaron las respuestas en `RESPUESTAS_EVALUACION_Y_EXAMEN.md` y `guia_defensa_examen.md` reemplazando los valores teóricos de biogás ($2.336,64\text{ m}^3\text{/d}$), potencia CHP ($208,41\text{ kW}$), y torta de fango ($28,70\text{ t/d}$) por los simulados/diseñados finales. Se corrigieron los retornos de escurridos a cabecera ($1.698,0\text{ m}^3\text{/d}$) y las dosificaciones de FeCl3 ($15\text{ g Fe/m}^3$). Se distinguió el resumen de potencia auxiliar ($162,60\text{ kW}$) de las bombas B1/B5 de la de transporte hidráulico principal ($98,22\text{ kW}$ y $875,87\text{ kW}$). |
| 2026-06-23 | Auditoría completa de RESPUESTAS_EVALUACION_Y_EXAMEN.md | El archivo contenía múltiples afirmaciones no respaldadas por las fuentes de verdad (4 decantadores, 2 digestores, Sección A-A', Det-01, potencias no verificadas, biofiltro no representado, etc.). | Se reescribió completamente el archivo verificando cada afirmación contra: (1) GPS-X OUTPUTS_PLUS max con hash `a578c03c...`, (2) Excel V11 hoja `28_Comparacion_GPSX`, (3) planos `EDAR_Perfiles_Plantas_v0.pdf` y su manifiesto. Se añadieron secciones de fuentes verificadas, tabla comparativa, advertencia de hash, legenda de cobertura y reglas de defensa. Se preservó backup del original. |
| 2026-06-23 | No modificar archivos fuera del alcance | La auditoría reveló datos incorrectos en el Excel V11 (hash registrado no coincide con hash real) y en los planos (elementos no representados). | Se documentó la discrepancia sin modificar los archivos fuente (GPS-X, Excel, PDF, DXF). Solo se modificó `RESPUESTAS_EVALUACION_Y_EXAMEN.md` y los Markdown de control. |
| 2026-06-23 | Marcado de cobertura para apartados de Parte 2 | 6 de los 14 apartados adicionales contenían datos no verificables o elementos no representados en planos. | Se asignó legenda: 3 ✅ CUBIERTO, 5 ⚠️ CUBIERTO PARCIALMENTE, 4 ❌ NO CERRADO, 2 🚫 NO REPRESENTADO. Esto permite al alumno defender con honestidad indicando qué está verificado y qué es preliminar. |
| 2026-06-23 | Detalle de MBR de doble tren paralelo (activo/reserva) | El usuario solicitó verificar si el proyecto final y GPS-X8 consideran 2 líneas en paralelo con un reactor MBR cada una. | Se actualizó la pregunta 6 en `RESPUESTAS_EVALUACION_Y_EXAMEN.md` detallando que el layout GPS-X8 (`PlantaEDAR.lyt` en OUTPUTS_PLUS) implementa dos reactores MBR paralelos (`Reactor MBR 4 zonas 1` y `2`), que el divisor de entrada simula la lógica activo/reserva (`Cfr&in&to&o1 = 1.0`), y que el Excel muestra solo el tren activo para evitar columnas de ceros. |
| 2026-06-23 | Sincronización de concentraciones de efluente reales | El cuestionario y la guía de defensa indicaban los límites de diseño (objetivos) en vez de los resultados reales simulados/calculados. | Se actualizaron las respuestas de la pregunta 10 y de la sección 3 en ambos documentos para reflejar los valores exactos de salida del Excel V11 y GPS-X Max (DBO5 2,02/2,10, SST 1,98/1,01, NT 5,29/5,06, PT 0,40/0,52 mg/L), asegurando consistencia numérica rigurosa. |
| 2026-06-23 | Reparación del Gemelo Digital 3D: z-fighting y traslape en zoom | El usuario reportó que al hacer zoom en el gemelo digital las figuras se traslapan con la topografía y no se ve bien. | Se añadió `polygonOffset: true, polygonOffsetFactor: 2.0, polygonOffsetUnits: 1.0` al material del terrain mesh en `web/app.js`. Esto empuja la profundidad del terreno hacia atrás en el depth buffer, eliminando la competencia de píxeles entre equipos y topografía. Adicionalmente se redujo la opacidad del terreno de 0.85 a 0.78 para menor obstrucción visual. |
| 2026-06-23 | Mejora de iluminación del gemelo digital 3D | El usuario reportó que el 3D se ve oscuro/mal iluminado. El `HemisphereLight.groundColor` era `0x667368` (gris-verde casi negro). | Se cambiaron 5 parámetros en `web/app.js`: `groundColor` a `0xaabbae`, `hemiLight.intensity` 0.58→0.72, `fillLight.intensity` 0.38→0.55, `ambientLight.intensity` 0.62→0.68, `toneMappingExposure` presentación 1.08→1.12. |
| 2026-06-23 | Corrección de datos erróneos en web/datos.html | La tabla de calidad de efluentes y la ficha de deshidratación en `datos.html` contenían valores que no coincidían con la fuente verificada (`RESPUESTAS_EVALUACION_Y_EXAMEN.md` auditado). | Se corrigieron 6 valores de efluentes (DBO5 2.51→2.02, DQO 20.97→22.26, SST 1.80→1.98, NH4-N 0.199→0.18, TN 5.30→5.29, TP 0.366→0.40 mg/L) y los datos de tortas (46.3→28.70 t/d, 3→2 viajes). |
| 2026-06-23 | Integración del contenido del examen en la web del gemelo digital | El usuario solicitó que el contenido de `RESPUESTAS_EVALUACION_Y_EXAMEN.md` estuviera disponible en la web del gemelo digital. | Se añadieron 3 secciones nuevas a `web/datos.html`: (1) Tabla Comparativa GPS-X8 Max / Excel V11 con 20 parámetros, (2) Reglas de Defensa (7 reglas en tarjetas), (3) Datos Clave del GPS-X8 con 17 variables `.lyt`. Todo el contenido proviene del archivo auditado y verificado. |
| 2026-06-24 | Topografía Voronoi y etiquetas de alto contraste con widget de control y examen acoplado | El usuario solicitó cambiar la topografía a polígonos de Thiessen, aumentar el texto de etiquetas y relleno negro, proveer la opción en una caja para ocultar/escalar y acoplar RESPUESTAS_EVALUACION_Y_EXAMEN como la primera sección de Datos y resultados. | Se modificó `scripts/generate_digital_twin.py` para usar `shapely.ops.voronoi_diagram` con jitter determinista, recortando las celdas a la caja de terreno y guardando `line_indices`. Se modificó `web/app.js` para renderizar únicamente los bordes de Voronoi (Thiessen) con `THREE.LineSegments` a partir de `line_indices`. Se rediseñó la estética de las etiquetas a texto negro sobre caja blanca y borde verde acentuado con mayor tamaño base. Se añadió un widget en `web/index.html` con checkbox para ocultar y slider para tamaño dinámico sincronizado. Se insertó el cuestionario de examen en formato acordeón colapsable al inicio de `web/datos.html`. |
| 2026-06-24 | Elevación de estructuras y visualización de etiquetas de infraestructura en 3D | Las tuberías del pretratamiento, decantación y reactores biológicos quedaban flotando en el aire desconectadas de las estructuras (cajas y cilindros), las cuales estaban completamente enterradas al tener una altura Ht menor o igual que la excavación E. Además, muchas etiquetas no se mostraban en el modo Presentación inicial al no estar listadas con prioridad >= 2. | Se incrementó la altura Ht de las estructuras en `scripts/generate_digital_twin.py` para sobresalir entre 0.2 m y 1.5 m del terreno, de modo que capturen las tuberías y salgan a la superficie. Se agregaron 14 etiquetas de equipos principales a `LABEL_PRIORITY` en `web/app.js` con prioridad 3. Se agregaron etiquetas y prioridades para la antorcha de biogás, chimenea de olores y edificio de soplantes. Se regeneró la geometría y se verificó con éxito. |
| 2026-06-24 | Subir únicamente el Gemelo Digital a GitHub | Optimizar el repositorio y cumplir con la directriz del usuario de incluir solo lo estrictamente necesario para la visualización web 3D. | Inicialización de Git local con un .gitignore estricto que excluye Excels pesados, manuales, capas GIS y archivos de simulación. Se subieron únicamente los archivos de web/ (Three.js, geometry.json), scripts/serve_web.py, y redireccionamiento en la raíz a GitHub (joseespinoza77/DSTA) en la rama main. |
| 2026-06-24 | Eliminar "lamelas"/"lamelar" de toda la documentación activa | El Jefe indicó que la palabra no debía aparecer. El diseño usa decantación primaria convencional (rectangular, por gravedad), no lamelar. El parámetro GPS-X `Caflat&o = 810.0` es área plana en proyección horizontal de un clarificador convencional, no superficie de placas. Modelo operativo: 2 decantadores de 810 m² c/u, pero solo L1 activa; L2 en reserva/mantenimiento (no suman 1.620 m² operativos). | 34 ocurrencias eliminadas en 5 archivos. Búsqueda final: 0 resultados de `lamelas|lamelar` en activos. Se revisó ortografía, gramática y teoría EDAR en todas las zonas modificadas. |
| 2026-06-24 | Pestaña Modelo GPS-X8 en el Gemelo Digital | El Jefe solicitó una tercera pestaña en la web mostrando las imágenes de simulación GPS-X 8.0 (OUTPUTS_PLUS) con escenarios de caudal máximo y promedio. | Se creó `web/gpsx.html` con galería responsiva y lightbox para 17 PNGs (854 KB total). Se agregó enlace "Modelo GPS-X8" en la barra de navegación de las tres páginas. Las imágenes se organizan en tres secciones: Layout general, Caudal Máximo (8 vars) y Caudal Promedio (8 vars). |
| 2026-06-24 | Pestaña Diagrama — Flujograma Integral | El Jefe solicitó una cuarta pestaña en la web del gemelo digital para mostrar el flujograma integral del proceso EDAR Silvouta. | Se creó `web/diagrama.html` con visualización centrada y lightbox para el PNG `flujograma_integral_edar_silvouta_ascii_letra_grande.png` copiado desde `0diagrama/` a `web/assets/images/diagrama/`. Se agregó enlace "Diagrama" en la barra de navegación de las cuatro páginas (`index.html`, `datos.html`, `gpsx.html`, `diagrama.html`). |



